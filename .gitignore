# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.pytest-tmp/
build/
dist/

# Vite 빌드 전용 의존성·산출물 (N-03)
node_modules/
build/web/
npm-debug.log*

# venv
.venv/
venv/
.uv-cache/
.uv-python/

# 생성 산출물 / 임시 데이터
out/
Results/
sample.xlsx
sample_bid_data.csv
*.tmp

# 비밀값(로컬 개발용 — 커밋 금지)
.secrets/
.env
.env.*
.claude/settings.local.json

# Claude 워크플로 실행 산출물(격리 worktree — 커밋 금지)
.claude/worktrees/

# Tool caches / reports
.coverage
coverage.xml
pytest.xml
web-artifact-identity.json
.ruff_cache/
.pyright/
.pytest-codex/
error.log

# 에이전트 도구 자산(제품 아님 — 머신 고정이라 커밋 금지)
# .claude/skills/ 는 .agents/skills/ 를 가리키는 절대경로 심링크다.
.agents/
.claude/skills/
skills-lock.json

# Installer output
installer-dist/

# 비공개 리서치 자료(공개 레포 제외) — 셋째 계보 앵커 조사 등
research-private/

# OS
Thumbs.db
desktop.ini
.DS_Store

# 패키징 산출물(빌드 시 자동 생성)
# packaging/*.ico 는 더 이상 빌드 생성물이 아니다 — 커밋된 정적 리소스(#258;
# filler 는 scripts/render_document_narmi_branding.py 산출). 미커밋이면 CI 빌드에
# 아이콘이 빠진 채 조용히 성공한다(spec 의 exists() 폴백).
*.spec.bak

# 브랜딩 초기 탐색 렌더(1MB 급 · 채택 안 된 방향) — 최종 산출물(*-final.*)과
# 채택된 방향안(document-narmi-logo-directions-v2.*)만 저장소에 남는다.
docs/branding/document-narmi-dashboard-mockup-*.png
docs/branding/document-narmi-icon-direction-*.png
docs/branding/document-narmi-abstract-icon-concepts-*.png
