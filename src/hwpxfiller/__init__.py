"""HWPX 누름틀 필드 주입기 — UnivContractor VBA HWPX 엔진의 Python 포트.

제품 버전은 여기 있지 않다. 단일 출처는 ``pyproject.toml`` 의 ``project.version`` 이고
PyInstaller·Inno 버전은 빌드가 거기서 생성한다 — 종전의 ``__version__`` 재수출은 소비자 0
인 채로 ``0.1.0`` 에 멈춰 있었다(#542 H-3, 그때 실제 버전은 ``0.3.0rc2``). 두 번째 자리는
읽는 사람이 없을 때조차 나중에 누가 믿을 값이 되므로 남기지 않는다.
"""

from __future__ import annotations
