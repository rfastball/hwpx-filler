/* 음성 대조 ⑨-나 — 주석이 역사적으로 `compat.js` 를 언급하고, import 는 다른 파일이다.
   `bootstrap.js` 머리말이 정확히 이 모양이라(승계 서사가 옛 이름을 적는다) 참조 축은
   **import 지정자**로만 물어야 한다. */
import { compatibility } from "./compatibility.js";

export const value = compatibility;
