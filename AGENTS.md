# 작업공간 지침

## 프론트엔드 시안

`docs/`, `web/`, `web-minimal/`의 HTML·CSS·JavaScript 시안과 프론트엔드 화면을 만들거나
수정할 때는 [프론트엔드 워크플로 시안 작성 원칙](docs/FRONTEND_WORKFLOW_PROTOTYPE_RULES.md)을
반드시 따른다.

특히 다음을 생략하지 않는다.

1. 수정 전에 현재 화면·상태·행동·mock·백엔드 capability를 조사한다.
2. 화면이 아니라 사용자 워크플로를 기준으로 설계한다.
3. 단계·전이·진입 조건·완료 조건·주 행동·복귀·실패·제한을 중앙 계약에 반영한다.
4. 화면 위치, 워크플로, 백엔드 처리, 입력, 저장, 오류 상태를 분리한다.
5. 주요 행동을 `supported`, `composed`, `unsupported`, `deferred`, `removed`로 분류한다.
6. 지원되지 않는 기능을 활성 행동이나 성공 mock으로 표현하지 않는다.
7. 각 단계에는 하나의 주 행동만 둔다.
8. 빈 상태, 대기, 처리 중, 성공, 부분 성공, 실패, 재시도, 저장 상태를 필요한 범위에서 구현한다.
9. 실패·복귀·재시도 때 사용자 입력과 이전 성공 결과를 보존한다.
10. UI, 워크플로 상태, mock 백엔드, capability, 샘플 데이터를 역할별로 분리한다.
11. 백엔드 원자 동작이나 내부 API 이름을 사용자 버튼·화면 이름으로 직접 노출하지 않는다.
12. 의미 HTML, 키보드 조작, label, 포커스, 오류 연결, 텍스트 상태 전달을 검증한다.
13. 수정 후 정적 계약 검사와 실제 브라우저 핵심 흐름을 다시 실행한다.
14. 감사 결과는 `유지`, `수정`, `재설계`로 나누고 근거·사용자 영향·수정 범위를 기록한다.

현재 핵심 워크플로 시안의 정본은 다음과 같다.

- `docs/core-workflow.md`
- `docs/core-workflow-ui-mvp-demo-v2.html`
- `docs/core-workflow-prototype/contracts.js`
- `docs/core-workflow-prototype/mock-backend.js`

기존 구조와 검증된 행동은 보존한다. 확인되지 않은 새 기능을 임의로 추가하지 않으며, 전면
재작성은 현재 구조로 워크플로 계약을 표현할 수 없을 때만 선택한다.
