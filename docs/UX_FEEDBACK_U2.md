# U2 실사용 피드백 라운드 — 트리아지와 판정

> **문서 상태:** 유효 결정 (**판정 완결 2026-07-30 · 착지 잔여 보유**)
> **권위 범위:** v6 표면 착지 이후 받은 실사용 피드백의 분류·판정·미결 목록
> **후속 정본:** 없음
> **편집 정책:** 항목이 닫힐 때마다 판정을 추가하고, **코드에 닿은 항목은 이름에 취소선 +
> 「조치완료(#PR · 커밋)」을 병기한다**. 이슈의 열림/닫힘 상태는 GitHub 이 소유한다.
> **취소선 단독은 뜻이 없다** — 뒤에 오는 굵은 표식(`조치완료` / `판정 이동 → §x.y` /
> `폐기`)이 뜻을 준다. **`폐기` 는 「안 하기로 했다」가 아니라 「다른 판정이 원인을 없애
> 지을 것이 남지 않았다」** 이고, 그 경우 **조치 표를 지운다** — 남겨 두면 다음 사람이 구현한다

v6 표면 전면 재작성(R1·F1~F8)이 완주한 직후 받은 **실사용자 피드백 34건**. 이전 U-라운드
(2026-07-23)는 재작성 **이전** 표면의 것이므로, 이번이 새 2탭 셸·라이브러리·드로어·작업대
형상에 대한 **첫 외부 실측**이다. 전 항목을 코드에 대조한 뒤 3분류했다.

**원문 전문 재대조(2026-07-29)** — 트리아지가 원문을 줄여 옮기면서 제안의 절반이나 기준면이
빠진 3건(§2.2 정렬 기준면 · §2.4 저장 버튼 라벨 · §5.2 종료 가드 우선순위)을 되돌렸다.
이 문서와 원문이 갈리면 **원문이 이긴다** — 여기 적힌 것은 원문의 요약이 아니라 그 위의
판정이므로, 요약 과정에서 사라진 조건은 판정의 근거까지 바꾼다.

재작성 자체의 근거 원장은 동결됐다 —
[`docs/archive/DATA_FIRST_INTEGRATION_MAP.md`](archive/DATA_FIRST_INTEGRATION_MAP.md).
아래에서 §10.x·§8.2 등으로 인용하는 절 번호는 전부 그 문서의 것이다.

---

## 1. 3분류 기준과 결과

| 분류 | 기준 | 건수 |
|---|---|---|
| ① | 문제가 실재하고 **고칠 방법이 명확** | 15 |
| ② | 문제가 **실재하지 않거나** 설계 의도로 소명됨 | 8 |
| ③ | 문제가 실재하나 **토의·검증이 남음** | 14 |

**계수 단위는 제보가 아니라 항목이다** — 위 셋의 합은 37이고 제보는 34건이다. 한 제보가 둘
이상을 말하면 판정 단위로 가른다: §2.4 「변경 저장」 행이 그 표본으로, *"원문은 한 제안 안에서
둘을 말한다"*(라벨 통일 + 상시 활성)라고 그 자리에 적혀 있다. 제보 수로 되돌려 세면 한쪽
판정이 다른 쪽에 가려지므로 **가른 채로 센다**.

**37항목 밖 1건** — §2.8(데이터 참조 분류 2축화)은 제보에 없던 실사용 발견이라 위 계수에 넣지
않는다. 원문 대조로 "누락이 아니라 부재"임을 확인했다. **그 1건은 2026-07-30 폐기됐다** —
§5.3 재편이 증상의 원인을 없앤다(판정은 §5.3 이 소유).

**이 문서가 소유하는 것은 판정과 착지 사실이다** — **무엇이 어긋났고 왜 그렇게 판정했는가**,
그리고 **그 판정이 어느 PR·커밋으로 코드에 닿았는가**. 이슈의 열림/닫힘 상태는 GitHub 이 진다.

가장 큰 소득은 **레이아웃 불만 3건이 회귀 버그 하나로 수렴**한 것이다. 사용자가 "음영블록·
여백을 달라"고 제안한 것은 새 디자인이 아니라 **두 슬라이스 전에 있던 것의 복구**다.

### 1.1 착지 대장 — 잔여를 세는 자리는 여기 하나다

| PR | 커밋 | 착지분 |
|---|---|---|
| #330 | `05c0c69` | §2.1 `.zone` 규칙군 복구 + 고아 class 게이트 |
| #331 | `4fcd68f` | §2.2 세션 액션바 정렬 기준면 · 작업대 좌우 끝단 · footer 묶음(3행) |
| #332 | `a7a59e0` | §2.3 앞 4행(「적용 범위」 삭제 · 0건 note 비움 · 「미리보기」 어휘 통일 · 「새로고침」 제거) |
| #333 | `a4ab7c1` | §2.4 4항목(작업명 발견성 + 저장 차단 조준 · dirty 게이트 · 일괄 재제안 · 현관 출구) |
| #334 | `3469d44` | §2.6 전부 + §2.2 「되돌리기 `↩` 과대」 |
| #335 | `5e0b7bb` | §2.10 C1(어휘 분리) · C3(지목 신설) |
| #336 | — | 착지 표식 규약(취소선 + 조치완료 병기)과 이 대장의 신설 |
| **PR 없음** | `329cd7a` | §5.1 원인 확정 기록 → §2.11 승계(문서만) |
| **PR 없음** | `e3577a1` | §2.11 전부 — 눌림 두 갈래 · 배경 축 4표면 · 고아 토큰 회수 · 검사 3층 · CI 전제 |
| **PR 없음** | `621c9b3` | §2.12~§2.20 판정 신설 + splitter·sticky 기각(문서만 — ③ 소진) |
| #353 | `6f4c646` | §2.12 삭제 문안 6자리 — 「휴지통」 어휘 하강(#345) |
| #354 | `da474fe` | §2.14 저장 착지 제자리 + §2.17 버리기 dirty 규칙(#337) |
| #355 | `29800cc` | §2.16 narrow — 폴더 일괄 등록(#339) |
| #356 | `4b5f7e7` | §2.9 종료 가드 성분 사망 + `silent_loss_by_contract` 선언(#344) |
| #357 | `4592f35` | §2.15 작업대 큐 고정 순서(#338) |
| #358 | `73627e6` | §2.19 데이터 표 열 선별(#341) |
| #361 | `0e5cac1` | §2.7 데이터 선택 면 단일 경로화 6행(#343) |
| #362 | `27f7b0a` | §5.3 판정 C·D — 데이터 축 정체성 + 결속 폐기(#347, 파괴적) |
| #359 | `1c1fe7d` | §4 판정 A~D·F + §2.20 — 존 사망·카드 승계·라이브러리 상세(#342) |
| #363 | `141fbf1` | §2.18 결과 처분 성분별 2분기(#340) |
| #364 | `59d1d8a` | §2.13 확인 면 1 pane + 필드축 ack 폐기(#346) |
| #366 | `824b40a` | §2.4 「이 데이터로 신규 작업」 + §4 판정 E — `DEFERRED_ENTRY_REASONS` 해제(#349) |
| #368 | `18f84c4` | §5.3 판정 B — 작업→템플릿 링크를 라이브러리 루트 상대키로(#348) |

> **머지 순서 ≠ 대장 순서.** 웨이브 1 11행(#353~#364)은 **병렬 5레인**(worktree 격리)의 산물이라 PR
> 번호와 착지 순서가 어긋난다(#359 는 #362 뒤에 머지됐다). 인용은 **커밋 해시**로 한다.

> **번호 경보(2026-07-30 정정).** 이 대장은 위 두 줄을 처음에 **`#337`·`#338`** 로 적었고 **커밋
> 메시지에도 그 번호가 박혔다**(`329cd7a`·`e3577a1`·`99cb7f9`). **그 PR 은 존재한 적이 없다** —
> 실제 마지막 PR 은 `#336` 이고 셋은 **master 직접 커밋**이다. 그리고 **2026-07-30 에 337·338 이
> 실제 이슈로 발급**됐으므로(§2.14·§2.17 / §2.15) **그 번호를 따라가면 다른 것이 나온다.**
> 커밋 메시지는 역사라 고치지 않고, **인용은 커밋 해시로 한다.** 발행 전 번호를 미리 적으면
> 나중에 그 번호가 남에게 발급된다.

**미착지 잔여**(2026-07-31 갱신 — 표식이 아니라 **생존 증거**가 잔여를 판정한다):

| 잔여 | 이슈 | 미착지 증거 |
|---|---|---|
| §6 매체 경보 — 101 스크린샷 재생성 | #352 | 커밋된 컷이 #332 문안(「생성 값 미리보기」)을 안 담는다 |

위 표에 있던 **13행이 2026-07-30 착지**했고(§2.7·§2.9·§2.12~§2.20·§4 A~D·F·§5.3 C·D) **남은 2행이
2026-07-31 착지**했다(§2.4·§4-E 는 #366 · `824b40a`, §5.3 판정 B 는 #368 · `18f84c4`) — 각 행의
커밋은 착지 대장에 있다. **잔여를 세는 자리는 이 표 하나**이므로 착지분은 대장으로 옮기고
여기서 내린다(두 곳이 같은 상태를 판정하지 않는다).

**착지가 낳은 별건**: **#365** 폴더 가져오기 재진술의 목적지 수치를 확정 지문에 결속(#339 의
리뷰 4라운드 분리분 — 파괴 축이 아니라 **예고 정확도** 축이고, 해법은 #347 이 세운
`confirm_basis` 기제에 편입하는 것이다).

**이슈 일괄 발행(2026-07-30)** — 위 표 + 별건 셋이 **#337~#352** 로 나갔고 마일스톤은
**「M. U2 실사용 피드백 착지」**(별건 둘은 마일스톤 밖)다. 별건: **#350** 휴지통 표면 ·
**#351** template+mapping 패키지(broad, 선행 #348) · **#352** 101 스크린샷 재생성(§6 매체 경보).
**판정은 이 문서가, 열림/닫힘은 GitHub 이 진다** — 이슈 본문에 판정을 옮겨 적었으므로 둘이
갈리면 **이 문서가 이긴다**.

`e3577a1` 이 §2.11 전부(전수조사 4표면 + 검사 3층 + 고아 토큰 회수 + CI 전제)를 담는다.

**라운드에서 빠진 것**: §2.8 — **폐기**(2026-07-30). 잔여가 아니라 **지을 것이 없어진 것**이라
위 표에서 내린다.

순서 제약은 하나다 — **§5.3 데이터 축 재편 → (§2.4 「이 데이터로 신규 작업」 · §4-E)** 가 같은
`DEFERRED_ENTRY_REASONS` 해제 한 번에 묶여 있다. **선행이 §2.8 에서 §5.3 으로 갈렸다**(폐기의
귀결): 「데이터를 이미 고른 채 신규 작업」이 파괴 확인으로 끝나던 원인은 자동등록이었고,
재편이 자동등록을 없앤다. 재편은 U2 밖 슬라이스라 **U2 라운드에서 닫히지 않는다고 적었으나,
마일스톤 M 이 그 슬라이스까지 함께 진행해 재편이 먼저 착지**(`27f7b0a`)했고 §2.4·§4-E 는 그
위에서 열려(#349) **뒤이어 착지**했다(#366 · `824b40a`). 제약 자체는 유효했다 — 순서만 지켜졌다.

**짓지 않기로 한 것·보류**: §2.3 「아직 한 번도…」의 자리(미조치 확정) · §2.10 C2 와 파일 이름의
자리(보류) · §2.5 동반 확인 대기(→ §2.14 는 `da474fe` 로 **착지**했고 실앱 한 바퀴는 **미실시**) · **splitter·sticky 사이드바 기각**(2026-07-30,
근거는 §5.2 그 행 — 「기각」은 「폐기」와 다르다: 원인이 없어진 것이 아니라 **다른 소유자가 이미
푼다**). **③ 미결은 §5** 가 소유한다 — 위 표의 14건 중
13건(「직접 등록」 처분 · 「파일 찾아보기」 고정 기회 · 종료 가드 처분 · **데이터 행 이동** ·
**휴지통** · **확인 2면 형상** · **저장 후 착지** · **복사 완료 건 후미 이동** · **부트스트래핑** ·
**취소·저장 문법** · **결과 처분** · **데이터 표 열 선별** · **템플릿 어포던스 이관**)이 ①로
승계되고 1건이 기각돼 **③ 미결은 0건**이다(2026-07-30 소진). ①로 간 뒤 열은 모두
2026-07-30 이다: 데이터 행 이동은 원인이 확정돼 §5.1 → **§2.11**, 휴지통은 3택이 닫혀 §5.2 →
**§2.12**, 확인 2면 형상은 §5.4 → **§2.13**(그 판정이 필드축 ack 폐기까지 데려갔다), 저장 후
착지는 되깎기 조건이 **이미 충족돼 있음**이 실측돼 §5.2 → **§2.14**, 복사 후미 이동은 2택이
**두 순서 중 하나 지우기**로 재기술돼 §5.2 → **§2.15**, 부트스트래핑은 범위가 **narrow 확정 ·
broad 별건**으로 갈려 §5.2 → **§2.16**, 취소·저장 문법은 어긋난 자리가 문법이 아니라 **dirty
가시성 규칙**임이 실측돼 §5.2 → **§2.17**, 결과 처분은 판정 G 가 **과적용**임이 드러나 성분별로
갈려 §5.2 → **§2.18**, 열 선별은 마일스톤 L 과 **축이 다름**이 확인돼 §5.2 → **§2.19**, 템플릿
어포던스는 **신설분이 한 곳**으로 좁혀져 §5.2 → **§2.20**.

**③이 소진된 뒤 이 문서가 세는 것은 「판정 잔여」가 아니라 「착지 잔여」다** — 위 미착지 표가
유일한 계수 자리이고, 판정은 전부 §2 에 있다. 새 실사용 발견이 오면 그것은 U2 의 잔여가 아니라
새 항목이다(계수를 되살려 섞지 않는다).

---

## 2. ① 실재 + 고칠 방법 명확

### ~~2.1 `.zone` CSS 유실 회귀 (P1, 단일 근본원인)~~ — **조치완료**(#330 · `05c0c69`)

> **귀인 정정(2026-07-29, 착수 실측).** 이 절은 처음에 *"9분할에서 이관 없이 소멸"* 이라고
> 적었다. git 실측은 다르다 — 기제가 **분할이 아니라 과다 삭제**다. 분류·조치는 그대로지만
> 후속 규칙이 달라지므로(아래) 근거를 고쳐 적는다.
>
> | 사실 | 증거 |
> |---|---|
> | `67894b0` 시점엔 아직 `app.css` 단일 파일이었다 | `git show 67894b0:web/css/job.css` → `fatal: ... exists on disk, but not in '67894b0'` |
> | 9분할은 **그 뒤** 별도 커밋이고 바이트 보존이었다 | `6b9a98e`(#322) |
> | 규칙은 `67894b0`(F6 PR-B, #317 「기안」 사망)에서 `.draft-duo` 를 지우는 **같은 hunk** 에 `-` 로 딸려나갔다 | `git show 67894b0 -- web/css/app.css` |

`67894b0`(F6 PR-B, #317)이 「기안」 화면을 죽이며 `.draft-duo` 계열을 지울 때 아래 규칙군이
**곁달려 함께** 삭제됐다. `.draft-duo` 는 화면과 함께 죽는 것이 맞았지만 이 넷은 살아 있는
「문서 만들기」가 쓰던 것이다. **착수 당시**(#330 복구 이전) `web/css/` 전체에 `.zone` 규칙이
0개인데 `web/css/job.css:217-224` 주석은 아직 그 규칙을 설명했다 — **주석만 살아남았다.**
(시점을 박아 둔다. 안 박으면 다음 사람이 이 문장을 지금의 실측으로 읽고 회귀를 다시 센다.)

```css
/* git show 67894b0~1:web/css/app.css:894-898 */
.job-zones{display:flex;flex-direction:column; ...}
.zone{padding:var(--sp-16) var(--sp-16) var(--sp-12);position:relative}
.zone + .zone{border-top:1px solid var(--a-border)}
.zone-cap{display:block;margin-bottom:var(--sp-10); ...}
```

| 피드백 | 이 회귀가 내는 증상 |
|---|---|
| 좌우 열 간격이 좁다 | 존 패딩 0 + `.data-grid` gap 미선언 → 콘텐츠가 `border-left` 1px에 맞붙음 |
| 섹션별 식별성 부족 | 인접 존 구분선 소멸 |
| 캡션이 눈에 안 들어온다 | `.zone-cap{display:block}` 소멸로 인라인 흐름에 섞임 |

**조치**: 규칙군을 `web/css/job.css`(존 소유자)로 복구. `draft-duo`·`.zone.workcard` 관련
규칙은 `draft` 화면 사망(F6 PR-B)과 함께 무효이므로 되살리지 않는다.

**`.data-grid` 열 간격은 명시하지 않는다**(착수 실측으로 뒤집힌 조치). 위 표의 「좌우 열
간격이 좁다」는 gap 미선언이 아니라 **존 패딩 0** 이 원인이다 — `.zone{padding:16px}` 이
돌아오면 두 열의 존이 각자 16px 를 갖고 구분선 양쪽에 서므로 해소된다. 원인이 하나인데
조치를 둘 넣으면 다음 사람이 어느 쪽이 일한 것인지 못 센다.

**반대 방향으로 남은 죽은 CSS 2건**(규칙은 있는데 생산자가 없다 — 위 그물이 안 잡는 축):

- `job.css:150-158` `.job-master`·`.master-splitter` — 좌 master 목록 사망(F2 PR-B)으로
  대응 DOM이 없다
- `job.css:178-196` `.job-row`·`.job-more`·`.job-grp` — 같은 사망의 유산인데 **지금은
  편집기 템플릿 탭(`.libselrow .job-more`)이 그 규칙을 먹고 산다**. 죽은 화면의 CSS 파일이
  산 화면을 부양하는 상태

**같은 커밋이 주석을 고쳐 썼다** — 이 사고에서 제일 나쁜 부분이다:

```diff
- ... zone-cap 은 절대 legend 대신 인라인 섹션 헤더. job·draft 공통(둘 다 .zone). */
+ ... zone-cap 은 절대 legend 대신 인라인 섹션 헤더. job 소유(.zone). */
```

규칙을 지운 손이 *"이제 job 이 `.zone` 을 소유한다"* 고 적어 뒀다. 소유자를 명시했으니
남은 사람은 규칙이 있다고 읽는다 — **흔적으로도 못 잡는다.**

**후속 규칙 — 「삭제는 의무를 상속한다」의 공유-표면판.** 표면을 죽일 때 위험한 것은 그
표면의 규칙이 아니라 **그 표면과 이웃이 나눠 쓰던 규칙**이다. 죽는 쪽만 보고 지우면 산 쪽이
말없이 벗겨진다. 이슈 **#325**(CSS 소유권 재정렬 — 선행 = 시각 회귀 그물)가 이 사고를
예언했고, 그물이 없어 **세 슬라이스**를 통과했다(사망 → 분할 → 착지).

그래서 복구와 **같은 PR** 에 당시 정적 고아-class 그물을 붙였다: DOM 이 내는 class 중 CSS
규칙이 **한 줄도 없는 것**을 실패로 만들었다. 당시 파일 manifest 검사는 링크 순서·전수 등재·
주석 균형만 보므로, 등재된 파일 **안에서** 규칙이 편집돼 사라지는 것은 못 봤다 — 세 검사가
모두 `.zone` 이 없는 상태에서 초록이었다. 이 범용 census들은 장기 컴팩트화에서 퇴역했고,
현재는 `frontend/src/main.js`의 import 정본과 위험별 JS·실렌더 소유자가 계약을 잇는다.

그물은 **한 방향**이다(DOM → CSS). 반대 방향(CSS 에만 있고 생산자가 없는 죽은 선택자 —
아래 2건)은 초기 오탐 정리가 별건 규모라 세우지 않았고, 그 사실을 게이트 docstring 에
적어 뒀다. 조용한 축소가 아니라 선언된 축소다.

### 2.2 배치·정렬·크기 — **4/4 조치완료**

| 항목 | 실측 원인 | 조치 |
|---|---|---|
| ~~미리보기/생성 버튼 좌정렬~~ | `.session-actionbar`(`job.css:71`)에 `justify-content` 미선언 | **좌섹션 안에서** 우정렬. 원문의 기준면은 *"좌섹션 horizontal splitter까지의 우중간정렬"* 이라 **화면 전체 우정렬이 아니다** — §5.2 splitter 항목과 기준면을 공유한다 — **조치완료**(#331 · `4fcd68f`) |
| ~~되돌리기 `↩` 과대~~ | `.btn.sm` = 본문 크기 + 좌우 패딩 10px(`base.css:150`), 아이콘 전용 클래스 미적용 | `.btn.icon` + `aria-label`(현재 `title`만) — **조치완료**(#334 · `3469d44`, §2.6 과 같은 PR — 자리·이름과 한 조치라 #331 이 아니다) |
| ~~온나라 작업대 좌우 끝단 불일치~~ | `#wbMapPanel{max-height:360px}` vs `.wb-preview{max-height:420px}` + `.wb-body{align-items:start}`(`tail.css:51-70`) | 캡 통일 + stretch. `#wbMapPanel`은 `scrollbar-gutter:stable` 셀렉터(`tail.css:37`)에도 누락 — **조치완료**(#331 · `4fcd68f`, 착지는 수치 통일이 아니라 `flex:1` 구조로) |
| ~~이전/다음 버튼 부유~~ | `.wb-foot .btn.primary{margin-left:auto}`만 있어 `복사`는 우측 끝, 이전/다음은 노트 뒤에 뜬다(`tail.css:74-77`) | 우 pane 소속으로 이동. **주의**: 「복사 후 다음」은 버튼이 아니라 체크박스 `#wbAdvance` — 피드백 문장과 DOM이 다르다 — **조치완료**(#331 · `4fcd68f`, `.wb-foot-nav` 묶음) |

### 2.3 문안·상태 표시 — **4/5 조치완료** (남은 1건은 미조치 확정)

| 항목 | 실측 | 조치 |
|---|---|---|
| ~~「적용 범위 — 이 작업의 기본 규칙」~~ | `screen_job.py:757` **하드코딩 상수 1개**. 이 축의 존재 이유였던 `runOverrides`는 §10.14에서 기각·사망했고, §10.15 판정 H는 작업대의 대응 배지를 "말할 상태가 없다"며 죽였는데 **드로어의 이 항목만 살아남았다** | 삭제 — 판정 일관성의 미이행분 회수 — **조치완료**(#332 · `a7a59e0`, 축 부재 되읽기 프로브 `app.py:2364` 동반) |
| ~~「선택한 문서의 이름이 모두 서로 다릅니다」~~ | `review_state.py:268-277` — 수렴 0건·경로 초과 0건일 때의 fallback note | 0건이면 비운다. §10.12 판정 K는 **건수 증거**를 요구했지 0건 성공 문장을 요구하지 않았다 — **조치완료**(#332 · `a7a59e0`) |
| ~~「미리보기」가 렌더링을 기대하게 함~~ | 코드가 "hwpx 본문은 앱에서 렌더 못 한다"를 명시(`job.js:456`, `screen_job.py:1029`). 모달 제목은 이미 「생성 값 미리보기」인데 **버튼만 「미리보기」** | 버튼 어휘를 모달 제목에 맞춰 통일. 렌더러 자리는 §5(③) 소관 — **조치완료**(#332 · `a7a59e0`, `index.html:372`) |
| ~~데이터 선택 「새로고침」~~ | 변호 실패 — `open()`이 열 때마다 이미 `pool.refresh`를 부른다(`data_picker.js:348`). 「모달을 띄워 둔 채 외부 변경」에만 유효한 잉여 어포던스 | 제거. 손상 격리 재검사 경로가 이 버튼에만 걸려 있지 않은지 확인 후 — **조치완료**(#332 · `a7a59e0`, `data_picker.js:348` 이 유일 재스캔 지점임을 주석으로 못 박음) |
| 「아직 한 번도 문서를 만들지 않은 작업입니다」의 **자리** | 문장 자체는 검토 요구의 **이유 문장**(§10.12 판정 N 3분기 중 하나)이라 살아남는다. 다만 그것이 **「확인할 변경」 캡션 아래** 있다 — 「한 번도 만들지 않았다」는 변경이 아니다 | **미조치 확정**(2026-07-29, 사용자) — 거슬리나 해가 작다. 근거와 짓지 않은 안 셋은 §2.10 말미 |

### 2.4 마법사·진입점 — **5/5 조치완료**(마지막 1건은 선행 착지 뒤 #366 · `824b40a`)

| 항목 | 실측 | 조치 |
|---|---|---|
| ~~작업명 입력란 발견성~~ | `index.html:392` — `<h1>` 안 `.title-input{border-color:transparent;background:transparent}`(`editor.css:141`). **보이는 라벨 0**, 플레이스홀더가 유일 안내. 저장 게이트는 이름 필수(`job_editor_state.py:63`)인데 차단 시 `#save-msg` 텍스트만 뜨고 **포커스 이동이 없다** | 시각 위계 보강 + 저장 차단 시 커서 이동·하이라이트 — **조치완료**(#333 · `a4ab7c1`). 조준은 링1 이 낸 `blocked_field`(`job_editor_state.py:35`)로 한다 — 링2 가 차단 **문구를 파싱하면** 문안을 고칠 때마다 조준이 조용히 깨진다 |
| ~~「변경 저장」 라벨 + 상시 활성~~ | 원문은 **한 제안 안에서 둘을 말한다** — *"별도의 '변경 저장'으로 표시를 바꿀 필요가 없다 (…) 기존 '저장'을 유지하되 실제 편집이 있을 때에만 회색→푸른색"*. 실측: 편집 = 「변경 저장」(`editor.js:883`) · 신규 = 「작업 저장」(`editor.js:890`)으로 **라벨이 갈려 있고**, `s.dirty`가 이미 있는데(`editor.js:880`) 두 버튼 모두 `disabled` 미적용 | ⑴ **편집 모드 한정** 회색→활성(신규 마법사는 항상 dirty라 적용하지 않는다). ⑵ 라벨 통일은 §10.13 **판정 E**("편집의 주 행동은 하나 = 「변경 저장」", `editor.js:877`) 대조가 선행 — 두 모드를 **이름이 아니라 상태로** 구별하자는 것이 원문 요지다 — **조치완료**(#333 · `a4ab7c1`): ⑴ 착지(`editor.js:940` dirty 게이트) · ⑵ 판정 E 대조 결과 **라벨 분리 유지로 닫힘** — 마지막 단계의 「작업 저장」은 완료 행동이지 변경 저장이 아니다 |
| ~~수동 재추천 버튼 부재~~ | 행 단위는 `↩`(`revert_source` → `resuggest_row`)로 **이미 있다** — 같은 피드백이 다른 항목에서 삭제를 요청한 바로 그 버튼이다 | **일괄 재추천만 신설.** 기존 수동 지정이 있으면 덮어쓰기 경고(`use-none` 선례 `editor.js:1061-1071` 재사용) — **조치완료**(#333 · `a4ab7c1`, `resuggest_all`). 착지는 행 단위와 **같다** — 「일괄로 한 것」과 「하나씩 N번 한 것」이 다르면 어느 쪽이 진짜인지 알 수 없다 |
| ~~현관 안내 링크 부재~~ | 데이터 선택 후 문서 작업으로 가는 안내가 평문 | 실진입 링크로 승격 — **조치완료**(#333 · `a4ab7c1`, `#jobNoDataExit`). 같은 출구가 「데이터는 있는데 쓸 작업이 0건」에도 선다(`job.js:1565`) |
| ~~「이 데이터로 신규 작업」~~ | `document_browser_new_work`가 `edit_session.py:68-76` `DEFERRED_ENTRY_REASONS`에 *"신규 작업 분기는 F8 소관"*으로 남아 fail-closed 차단 중. F8은 종결됐다 | 그 줄 삭제가 배선의 전제. 마법사 형태는 **기존 마법사 + 2단계에 메인 데이터 앵커**(멘탈모델 일관성, 사용자 확정). **§5.3 데이터 축 재편이 선행 조건이다**(2026-07-30 갈림 — 원래 §2.8 이었다) — 이 동선은 정의상 「데이터를 이미 고른 채 신규 작업을 만드는 것」이라, 지금 열면 새 진입점을 **전원이 파괴 확인으로 끝낸다**. 그 확인의 원인은 저장 시 자동등록이고, 재편이 자동등록을 없앤다. **조치완료**(#366 · `824b40a` — `DEFERRED_ENTRY_REASONS` 해제. 선행이던 §5.3 재편은 `27f7b0a` 로 착지했다): #333 이 나머지 넷만 착지시키며 유보 근거를 적어 뒀다 — *"`document_browser_new_work` 는 발신자가 아직 0곳이라 그 줄만 지워도 열리는 것이 없다"* |

### 2.5 동반 해소 — **선행(§2.14) 착지 · 확인 미실시**(2026-07-30)

**「저장되었다는 명시적 안내가 없다」 — 안내는 실재한다.** 본문 최상단 `note quiet`
(`editor.js:213`) + 헤더 상태칩 「저장됨 · 템플릿 r… · 연결 r…」(`editor.js:144`). 못 본
원인은 **저장 후 섹션 점프**(§5 ③ → 판정은 **§2.14**)가 시선을 옮기기 때문이다. 그 항목이 닫힐 때
동반 확인하고, 그때도 안 보이면 별건으로 승격한다. **§2.14 가 그 점프를 없앴으므로 확인의 매체는
실앱 한 바퀴다**(사용자 확정 2026-07-30) — 안내 자리를 함께 옮기지 않는 이유는 §2.1 과 같다:
원인이 하나인데 조치를 둘 넣으면 어느 쪽이 일한 것인지 못 센다.

**상태(2026-07-30)**: 대기 조건이던 §2.14 는 착지했다(#354 · `da474fe`). **그러나 그 실앱 한
바퀴는 아직 돌지 않았다** — 이 항목은 조치완료가 아니라 **확인 미실시**다. 방법은 §6 「§2.5 동반」
행이 소유하고, 그 한 바퀴에서 여전히 안 보이면 여기 적힌 대로 별건으로 승격한다.

### ~~2.6 되돌리기 `↩` — 기능 변경 0, 이름과 자리만 (사용자 확정 2026-07-29)~~ — **조치완료**(#334 · `3469d44`)

②에서 승계. 하는 일이 이미 「이 행 자동 제안 다시 받기」(`resuggest_row`)인데 글리프가 undo 라
**기능이 이름에 가려졌다**. 폭 비용은 없다 — `table.map` 은 `table-layout:fixed` 고 3열은
**170px 고정**(`editor.css:55`)이라 라벨 길이가 열폭에 미치는 영향이 0 이다.

**대신 자리가 없다.** `.sel{width:100%}`(`editor.css:61`)이 가용 154px 를 전부 먹고 버튼이 그
뒤에 인라인으로 붙어(`editor.js:753`) **두 번째 줄로 밀린다** — 원문의 *"과하게 커서 UI 깨짐"*
이 가리키는 것이 이 줄바꿈이다. `.btn{font-size:var(--fs-body)}` 14px 이 표 본문 13px 보다 큰
것이 그 위에 얹힌다.

| 조치 | 값 |
|---|---|
| 셀 구조 | `<td>` 안을 `.srcwrap`(flex·gap 4px)로 감싸고 `.sel{flex:1;min-width:0;width:auto}` — **`td` 에 flex 를 걸면 `table-layout:fixed` 정렬이 깨진다** |
| 버튼 | `.btn.sm` → `.btn.icon`(`base.css:151` 기존) + `font-size:var(--fs-dense)` + `aria-label="이 행 자동 제안 다시 받기"` |
| 글리프 | `↩` → **`↻`(U+21BB)**. `⟳`(U+27F3)는 커버리지가 얕아 **두부로 조용히 떨어진다**(근거는 아래 실측 — 결론은 같고 이유가 다르다) |
| 발견성 | 행은 아이콘 유지(select 실폭 154 → ~118px). **텍스트는 §2.4 일괄 버튼이 진다** — 「자동 제안 다시 받기」 |

라벨을 행마다 텍스트로 달지 않는 이유는 취향이 아니라 154px 산수다 — 「자동 제안」이면 select
가 ~90px 로 밀려 **데이터 열 이름이 안 보인다**.

**글리프 커버리지 실측**(착수 시 cmap 대조 — 「Segoe UI 계열이 얕다」는 추정을 재 봤다):

| 글꼴 | `↩` U+21A9 | `↻` U+21BB | `⟳` U+27F3 |
|---|---|---|---|
| **Pretendard(동봉)** — 앱의 1순위 | O | **O** | **X** |
| Segoe UI | X | X | X |
| Segoe UI Symbol(폴백) | O | O | O |

**결론은 같고 이유가 더 강하다.** 갈리는 지점은 Segoe UI 계열이 아니라 **동봉 글꼴**이다 —
`↻` 는 우리가 배포하는 글꼴 안에 있어 폴백 없이 그려지고, `⟳` 는 없어서 시스템 폴백에
의존한다. 포터블 배포에서 글꼴을 동봉한 이유가 바로 그 의존을 없애는 것이므로, 시스템
폴백에 기대는 글리프는 **동봉의 목적을 무르는** 선택이다. (Segoe UI 는 셋 다 없어 지금
쓰는 `↩` 도 이미 폴백으로 그려지고 있었다.)

계약 영향 0: 이 표는 JS 생성이라 정적 DOM 계약 밖이고, selftest 프로브는
`table.map [data-act="revert-source"]` 로 매칭한다(`app.py:1729`).

**줄바꿈 해소는 실렌더 기하로 잰다.** 정적 CSS 검사로는 못 보는 결함이라(규칙은 다 있는데
폭 계산 결과로만 밀린다) 칩-라이브 프로브에 측정을 얹었다 — 버튼이 있는 **수동 행**과 없는
**제안 행**의 「데이터 열」 칸 높이가 같은지, 그리고 버튼과 select 의 세로 중심이 붙어 있는지.
101 하니스로는 못 보는 자리다: 그 흐름은 전 행이 확정으로 끝나 버튼이 아예 안 뜬다.

### ~~2.7 데이터 선택 면의 단일 경로화 (사용자 확정 2026-07-29)~~ — **조치완료**(#361 · `0e5cac1`)

②의 두 항목(「파일 찾아보기 ≡ 직접등록」·「등록 후 바로 메인으로 나감」)이 **한 조치로 닫힌다**.

**출발점은 이미 적힌 약속이다.** `index.html:591` 이 *"한 번만 쓸 파일. 고른 뒤 「이 데이터
고정」으로 남길 수 있습니다"* 라고 말해 두고, `browseFile()` 은 성공하면 `finish(r)` 로 면을
닫는다(`data_picker.js:214`) — **안내를 읽은 직후 그 기능이 사라진다.** 새 설계가 아니라
문안의 미이행분이다.

| # | 조치 | 근거 |
|---|---|---|
| 1 | **찾아보기 성공 시 면을 닫지 않는다.** 「현재 데이터」가 방금 고른 파일로 재진술되고 그 자리에 「이 데이터 고정…」이 선다 | 계약면 3 의 *"성사 뒤에만 닫는다"* 는 **허가지 의무가 아니다**(실패 때 닫지 말라는 계약) |
| 2 | 고정 목록 선택(`mountPinned`)은 **지금처럼 닫는다** | 이미 고정된 것이라 남은 결정이 없다. 「끝난 선택은 닫히고, 결정이 남은 선택은 남는다」 |
| 3 | `pick_data_file`·`load_data_sheet` 반환을 **descriptor**(`label`·`path`·`sheet`·`rows`)로 | 고정 버튼은 `origin==="file" && path` 로 서는데(`data_picker.js:73`) JS 엔 path 가 없다. 푸시 도착을 기다리면 **발신 순서에 기대는 것** — 마운트한 그 호출이 결과를 말하게 한다 |
| 4 | 「＋ 직접 등록…」(`#dataPickerRegister`) **삭제** | 고정 다이얼로그와 **같은 모달**(`#poolRegModal`)이라 이름·시트·메모가 전부 승계된다. 고유 기능은 **「마운트하지 않고 등록」 하나**인데, 그것이 가능한 이유가 `pickPoolDataFile` 이 **경로만 반환하고 읽지 않기**(`bridge.js:68`) 때문 — **유일한 고유 기능이 곧 그 결함** |
| 5 | pin 모드에서 path·sheet **읽기전용** + 폼 안 `#poolRegBrowse` 감춤 | 「이 데이터」가 방금 확인한 그 데이터라는 말이 참이 된다. **어휘 충돌의 당사자가 이 버튼** |
| 6 | 이중 고정은 **회차 기억**으로 막는다 — 고정 성공 시 버튼 자리에 「고정됨: 이름」 | 풀 중복 판정이 **이름 기준**이라(`dataset_pool_state.py:205-214`) 면이 열린 채 두 번 누르면 같은 파일이 2건이 된다. 자기가 방금 한 행동의 결과 보고라 링2 재판정이 아니다 |

**사용자가 내주는 것**(명시 확인): ⑴ 한 번 쓰고 마는 사람에게 「닫기」 1클릭 ⑵ **「지금 데이터를
건드리지 않고 다음 파일을 미리 등록」 동선의 소멸** — 등록하려면 반드시 읽어야 하고, 읽으면
현재 데이터가 바뀌어 손실 가드가 뜬다. 그 대신 **풀에 들어가는 항목은 전부 「한 번은 실제로
읽힌 것」** 이 되어 지연 실패가 구조적으로 사라진다.

**삭제가 상속하는 의무**: `#poolRegBrowse` 를 지우기 전에 고정 목록의 「다시 연결」 경로
(`onPinnedClick` act `relink`)가 그 버튼을 쓰는지 확인한다. 쓰면 `pickPoolDataFile`·
`pick_pool_data_file` 은 살리고 pin 모드에서만 감춘다. 안 쓰면 브리지 메서드까지 함께 죽인다.
§2.3 의 「새로고침」 제거와 합쳐지면 `고정한 데이터` 캡션의 `.acts` span 이 통째로 비므로 함께
지운다(`index.html:581-584`).

### ~~2.8 데이터 참조 분류 2축화~~ — **폐기**(2026-07-30, 사용자 확정 — 원인 소멸 · 판정 → §5.3)

원문 34건에 **없는** 항목이다(대조 확인). 실사용 중 발견돼 편입했고, **진단은 유효한 채로
조치만 폐기한다** — §5.3 재편(작업↔데이터 결속 폐기)이 이 증상의 **원인**을 없앤다.

**증상(기록으로 남긴다)**: 데이터A 로 작업을 하나 저장해 둔 뒤 **같은 데이터A 로 두 번째
작업을 만들면** 저장 시점에 danger 확인이 뜬다 — *"등록 데이터 '데이터A' 이 이미
있습니다(…). 이 작업의 데이터 참조로 덮어씁니다. 계속할까요?"*(`screen_editor.py:1628-1633`).
매체와 무관하다(게이트 조건은 `if self.data_path:` 하나). 사용자 상황에서는 **눌러도 아무것도
안 바뀐다** — 무해한데 겁주는 상태, 경보 인플레이션이다.

**계보가 곧 사망 근거다**: ⑴ 작업이 선언한 데이터를 풀에 자동등록한다(#18·#26 —
`default_dataset_ref` **자동 조준의 전제**) ⑵ 이름 기본값이 **파일 스템**이라 같은 파일 → 같은
이름은 필연 ⑶ 동명은 레지스트리가 자기-갱신으로 **조용히 통과**시켜 `opts` 가 덮이므로 확인을
승격했다(`:1583`). §5.3 이 ⑴의 **목적**(자동 조준)을 폐기하면 자동등록은 존재 이유를 잃고,
등록은 데이터 선택 면의 「이 데이터 고정」 **명시 행동** 하나로 남는다(§2.7 1행이 그 자리를 이미
만든다). **게이트가 발화하는 지점 자체가 없어진다.**

**막으려던 위험도 함께 사라진다.** 「매출」 항목의 참조가 갈아끼워져 *"그 이름을 쓰던 다른
작업들이 조용히 다른 데이터를 본다"* 가 이 게이트의 정당화였는데, 재편 뒤에는 **그 이름을 드는
작업이 0곳**이다(`Job.default_dataset_ref` 폐기 — 저장소 전수 대조상 풀 이름을 외래참조로 드는
곳은 그 필드 하나였다. 원장은 들지 않는다).

**짓지 않는 것**(편집 정책에 따라 조치 표를 **삭제**한다): 2축(이름·참조) 4분면 분류와 풀 전용
참조 역조회 함수. 남겨 두면 다음 사람이 곧 사라질 게이트의 문안을 다듬는다.

**폐기가 상속하는 의무 둘**:

- §2.4 「이 데이터로 신규 작업」의 선행이 §2.8 에서 **§5.3 으로 갈린다**(§1.1 순서 제약 갱신).
- §2.7 6행(이중 고정을 **회차 기억**으로 막음)은 이 라운드에서 **적힌 대로 착지한다**. 다만
  재편이 중복 판정을 **경로 기준**으로 바꾸면 같은 경로 재등록이 같은 항목이 되어 그 기억은
  잉여가 된다 — **그때 되깎는다**. 여기서 미리 지우지 않는 이유는 §2.7 이 먼저 착지하기
  때문이고, 폐기된 §2.8 이 「공짜로 준다」고 했던 `pinned_as` 백엔드 판정도 **오지 않는다**.

### ~~2.9 종료 가드 — 「문서 만들기」 성분 사망, 조용한 손실은 계약이다 (사용자 확정 2026-07-29)~~ — **조치완료**(#356 · `4b5f7e7`)

②에서 승계. 원문 우선순위 ①세션 복원 → ②조용한 손실 → ③가드 조건 축소 중 **②로 확정**한다.

**③ 가드 조건 축소는 기각.** 술어(`data_zone.py:383-393`)는 이미 정밀하다 — 전체 선택·필터
정의 유래·마지막 생성분이 전부 무장에서 빠져 남는 것은 「손으로 고른 몇 행을 **안 쓰고**
나간다」 하나뿐이다. 더 줄이면 진짜 손실도 같이 놓친다. **처분 대상은 조건이 아니라 참여
자체다.**

**① 세션 복원을 안 고른 근거**(짓지 않기로 한 것도 기록한다): 복원이 이 가드를 실제로
닫으려면 술어 성분인 **선택 집합**을 덮어야 하는데, 선택은 **인덱스**이고 데이터 참조는
경로만 저장돼 실행 때 다시 읽힌다(`core/dataset_pool.py` 계약). 파일이 그사이 바뀌면 복원된
인덱스가 **다른 행을 조용히 가리킨다** — 가드 하나를 없애고 더 나쁜 침묵을 얻는 거래다.
정직하게 지으려면 행 수·열 헤더 지문 + 불일치 시 **복원 거절 재진술**까지 따라와 슬라이스
하나 규모가 된다. 계약 미착지분 `RestoreMountedData`/`SaveMountedDataRef`(§10.3)는 이
결정으로 **소비자를 잃는다**.

**사용자 계약**(원문 그대로) — *"명시적 종료 선언으로 받아들임 + 종료시 진행중 작업 정보를
보존하지 않는다는 사용자 계약"*.

| | |
|---|---|
| 죽는 것 | `screen_job.py:1928` `close_guard_reason` + 문안 「작업 화면의 완료하지 않은 선택」 |
| **사는 것** | `_guard_state`·`session_guard_for` — 홈 삭제 가드(#268)와 데이터 재겨눔·재연결 **사전 확인**(`_do_guard_state`)이 계속 소비한다. 소비자 셋 중 **하나만** 끊는다 |
| 무변경 | `close_guard_state` 참여 프로토콜·`Modal.confirm` 문안·`app.py` |
| 남는 참여자 | editor(「저장하지 않은 작업 편집」) · workbench(「미저장 연결 변경 또는 복사 진행」) — **둘 다 미저장 산출물**이지 재현 가능한 선택이 아니다. **국경이 여기다** |

**배제는 선언이어야 한다.** `test_close_guard_is_a_protocol_every_session_surface_joins` 의
`not_session` 표가 이미 그 자리를 지어 뒀지만, 그 표의 사유는 *"창을 닫아도 잃을 것이 없다"*
다 — job 은 **잃는다**. 같은 표에 넣으면 테스트가 거짓말을 하게 되므로 **두 번째 표**를
세운다(`silent_loss_by_contract = {"job": …}`). 「잃을 것이 없다」와 「잃지만 계약으로 조용히
잃는다」는 다른 선언이고, confirm-or-alarm 예외는 **숨기는 대신 표에 적히는 방식으로** 존재한다.

**예외의 성격 — 원칙에 예외 2호가 생겼다.** 기존 완화 조항(R-flow 결정 31: 전면 가시성 ∧
무반복 ∧ 검토 내장 표면의 「틀리면 보이는 추측」)과 **다른 계열**이다. 이건 추측이 아니라
손실이고, 정당화는 가시성이 아니라 **행위의 명시성**(X 클릭 = 종료 선언)이다. 두 예외를 한
조항으로 뭉치지 않는다 — 뭉치면 다음 조용한 경로가 둘 중 아무 쪽에나 기대어 들어온다.

### 2.10 두 확인 게이트 — 어휘를 가르고 지목을 준다 (사용자 확정 2026-07-29)

②에서 승계. 제보는 *"두 트리거의 차이를 소명하라"* 였고 §3 은 **소명을 요구했다는 사실이
결함**이라고 판정한 뒤 ③(확인 2면 재편 — 지금 §5.4)으로 넘겼다. 그 뒤 토의에서 처분이 갈렸다 —
**형상은 ③에 남기고 어휘·지목만 여기로 내린다.**

**국소로 가는 근거는 겹침의 수명이다.** 두 확인이 함께 서는 것은 **작업당 정확히 한 번**,
처음 만드는 작업에서뿐이다(§3 참고 · §10.12 판정 N: 업그레이드 비용은 작업당 1회). 한 번짜리
마찰에 표면 재편을 물리면 비용이 마찰보다 크다. 사용자가 잃는 것도 시간이 아니라
*"내가 방금 한 걸 또 하라는 건가"* 라는 신뢰다.

**증상을 원인 셋으로 가른다** — 셋 다 국소다.

| | 원인 | 처분 |
|---|---|---|
| ~~C1~~ | **이름이 같다.** 필드축 ack 와 규칙축 review 가 표면에서 둘 다 「확인」 | **어휘 분리** — **조치완료**(#335 · `5e0b7bb`) |
| C2 | **두 번째가 불시에 나타난다.** `_compose_gate` 는 서열 최상위 **하나만** 말하므로 빈 값 ack 를 다 끝낸 그 순간에야 검토 요구가 처음 뜬다 — 골대가 움직인다 | **보류**(아래) |
| ~~C3~~ | **내용이 같아 보인다.** 둘 다 "생성 전에 값을 본다" | **지목 신설** — **조치완료**(#335 · `5e0b7bb`) |

**C3 은 느낌이 아니라 사실 문제이기도 하다.** 첫 실행 드로어에서 거울이 못 보여주는 유일한
것은 **파일 이름 축**인데(거울은 필드×집계값뿐 — `_field_value_display`), 승인 버튼은
「결과 확인 완료」라고만 말해 그것을 안 가리키고 파일 이름은 `preview-foot` 구석에 가장 작게
선다. 그래서 사용자 눈에는 방금 거울에서 본 값을 한 번 더 보는 화면이다.

**어휘 — 「확인」은 필드축이 단독으로 갖고 규칙축은 「승인」으로 간다.** 아래 표 전체가
**착지했다**(#335 · `5e0b7bb`) — 4자리 모두 「승인」이고 필드축은 「확인」 그대로다.

| 자리 | 지금 | 바뀐 뒤 |
|---|---|---|
| 표지 `#jobReviewFlag` | `확인 필요` | `승인 필요` |
| 게이트 `review_gate_text`(`review_state.py:323`) | …「미리보기에서 결과를 **확인**해야 생성할 수 있습니다.」 | …「미리보기에서 나갈 **이름과 값을 승인**해야 생성할 수 있습니다.」 |
| 드로어 부제(`index.html:609`) | 「실제로 문서에 들어갈 값입니다. 미리보기를 여는 것과 **확인**은 다른 일입니다.」 | 「이 **이름**으로 이 값이 나갑니다. 미리보기를 여는 것과 **승인**은 다른 일입니다.」 |
| 승인 버튼(`index.html:648`) | 「결과 **확인** 완료」 | 「**이 이름과 값으로 승인**」 |
| 거울 존·배지·빈 값 게이트 | 「확인」 | **무변경** |

**어휘를 새로 짓는 것이 아니라 링1 이름을 표면으로 올리는 것이다** — 코드는 이미 `approve`·
`PreviewApproved` 불변식(§13-4)으로 그렇게 부르고, 표면만 두 축을 한 단어로 눌렀다. 부수로
버튼이 스타일 가이드 §84(버튼=동사)에 맞는다.

**탈락한 두 후보와 근거**(어휘는 점유 대조 없이 고르면 충돌을 옮길 뿐이다):

- **「검토」** — `work_mode.py:44` 「온나라 기안 **검토**·복사」가 점유. 코드 이름(`review_state`)
  과는 맞지만 표면에서 다른 축에 이미 가 있다
- **「확정」** — 매핑 축이 점유(*"모든 행을 검토·확정해야 저장으로 진행"*, `COPY_STYLE_GUIDE` §76)

**곁다리로 함께 풀리는 충돌**: 라이브러리 탭 「확인 필요」(후보 건전성 — `work_candidates.py`)와
`#jobReviewFlag`(규칙축)가 지금 **글자까지 같다**. 규칙축이 「승인」으로 옮겨가면 별도 조치
없이 사라진다.

**C2 보류 근거.** 잔여를 미리 보이려면 게이트가 목록이 되는데, 게이트는 **단일 산출**이 계약
이고(UD-06: 버튼 비활성 + 인라인 사유 하나) 실제 서열은 드리프트·파일명·빈 값 ack·저장 폴더·
선택 0건·검토 요구까지 최대 여러 단이라 "남은 것 전부"는 국소수술이 아니게 된다. C1·C3 착지
뒤에도 골대 느낌이 남는지 실앱 한 바퀴로 재평가한다.

**파일 이름의 자리는 이번에 옮기지 않는다.** `preview-foot` → 상단 승격은 C3 을 더 확실히
하지만 드로어 배치를 건드리므로 §2.2 와 함께 판단한다. 이번 조치는 **문안까지**다.

**미조치 항목의 기록**(§2.3 마지막 행 — 「아직 한 번도…」가 「확인할 변경」 캡션 아래 있는 것).
사용자 확정으로 **미조치**다. 근거는 거슬림이 실재하나 해가 작다는 것. 짓지 않기로 한 안 셋을
남긴다 — 다시 발견될 때 세 번 세지 않으려고 적는다:

1. 캡션을 사유별로 갈리게(링1이 캡션 문안까지 소유) — 페이로드 키 1개 신설, 문안 3벌 유지
2. 캡션 중립화(「확인할 것」) — rows 가 진짜 before/after 변경일 때 그 이름을 잃는다
3. reason 을 섹션 밖 드로어 상단으로(증거 섹션은 증거가 있을 때만) — 정적 DOM 계약 갱신

3안이 가장 정직했다(reason 은 섹션의 부제가 아니라 드로어 전체의 전제이고 게이트와 문장을
공유한다). §2.3 의 note 0건 비움과 합치면 첫 실행에서 증거 섹션이 통째로 비어 자연히 사라지는
것도 그 안의 소득이었다. **비용 대비로 접었을 뿐 판정이 뒤집힌 것이 아니다.**

**삭제·개명이 상속하는 의무**: `app.py:2156` 셀프테스트 스텁이 게이트 문안 **사본**을 들고 있어
함께 갱신한다. 표지 텍스트는 정적 DOM 계약이 잡고, 링1 함수·클래스 이름(`review_state`·
`review_gate_text`·`ReviewRequirement`)은 **바꾸지 않는다** — 표면 어휘 교체지 판정 축 개명이
아니다.

### ~~2.11 눌림 표지가 전폭 행을 부순다 — `tr` 에 `transform:scale`~~ — **조치완료**(`e3577a1` · PR 없음, §1.1 번호 경보)

③에서 승계(§5.1 이 「원인 미상」으로 들고 있던 항목). `web/css/base.css:106` 이
`.jobtb tbody tr:active{transform:scale(.97)}` 이고 `:101-103` 이
`transition:transform var(--dur-press)` 를 붙인다. `<tr>` 에 **중심 기준 축소**가 걸려 누르는
동안 행이 양쪽에서 안으로 140ms 에 걸쳐 모인다.

**실측**(실 배포 CSS 를 `index.html` 순서로 링크한 하니스 · 990px 행 · `mousedown` 유지):

| | `reduce` | `no-preference` |
|---|---|---|
| computed transform | `none` | `matrix(0.97, 0, 0, 0.97, 0, 0)` |
| 행 좌변 | Δ 0 | **+14.85px** |
| 행 우변 | Δ 0 | **−14.85px** |
| 행 폭 | Δ 0 | −29.70px |
| 글자 상자(파일 이름) 폭 · 높이 | Δ 0 | −3.87px · −0.58px |
| 열 정렬 이탈(행 첫 칸 좌변 − sticky `th` 좌변) | Δ 0 | **+14.85px** |

제보 넷이 전부 이 한 규칙의 숫자다 — 「중앙으로 모인다」는 좌우 편차가 **정확히 대칭**인 것,
「우측으로 움직인다」는 좌변 +14.85, 「글씨가 수축」은 글자 상자가 실제로 작아지는 것,
「클릭이 어긋난다」는 히트테스트가 변형된 기하로 가는 것 + 열이 머리와 14.85px 갈리는 것.

**기제는 토큰이 아니라 상자 크기다.** 32px 버튼에서 3% 는 1px — 정확한 눌림이다. 990px 행에서
3% 는 30px — 레이아웃 파손이다. **버튼 관용구가 전폭 행에 그대로 복사됐다.**

**전수조사(2026-07-30) — 제보보다 넓다.** 실 `index.html`·1440×900·`reduce`/`no-preference`
양측. 15표적 중 **파손 4**, 무해·허용 10, 죽은 선택자 1. 원 제보의 표 행은 **셋째**다.

| 파손 | 실폭 | Δ좌변 | Δ폭 | |
|---|---|---|---|---|
| `.job-grp-head` | 1170px | **+17.56px** | −35.11 | 라이브러리 그룹 머리 — **제보에 없던 최악** |
| `.mir-row.miss` | 920px | +13.80px | −27.60 | 거울 빈 값 행 = **ack 게이트를 푸는 유일 표면**(§10.6 결정 3) |
| `.jobtb tbody tr` | 910px | +13.65px | −27.30 | 원 제보 |
| `.ctx-menu button` | 140px | +2.10px | −4.20 | 채움비 0.93 — 「작은 상자」로 분류돼 있었으나 메뉴 폭을 **채운다** |

무해·허용 10건은 전부 자기 폭만 갖는다(`.btn` +2.22 · `.navbtn` +1.68 · `button.pill` +1.19 ·
`.tpl-assign`·`.wstep-tab.as-tab` +1.15 · `.shell-tool` +1.01 · `.fico` +0.29 · `.job-more`·
`.grp-more` +0.25 · `.fchip button` +0.10). 등장 모션은 안전하다 — `.ctx-menu`·`.colpanel` 의
`transform-origin` 이 `0% 0%` 라 좌변이 안 움직이고, `.modal-card` 는 뷰포트 오버레이라 공유
기준면이 없다.

**판정 기준은 「컨테이너를 채우는가」다**(폭 자체가 아니라). 채우는 요소는 이웃·머리와 기준면을
공유하므로 중심 축소가 곧 정렬 이탈이다. `.ctx-menu button` 이 140px 로 작은데도 파손인 이유이고,
`.btn` 이 148px 로 비슷한데 무해한 이유다.

**같은 계열 셋을 더 찾았다 — 「선언은 살고 결과는 죽는다」.**

- **`.job-item`** — 눌림 계약 4자리에 이름이 있는데 **산출자가 웹 자산 전체에 0곳**이다
  (F2 PR-B 로 좌 master 목록 사망). 평면 계약이 죽은 선택자를 부양했다.
- **`badge.missing_press`** — `design_tokens.json` 에 **두 테마 다 손으로 지정**돼 있는데
  `gen_design_tokens.py` 매핑이 없어 `tokens.css` 에 **방출조차 안 됐다**. 사용처 0곳. 즉 눌림을
  배경 축으로 하는 설계가 **이미 있었고 버려졌다** — `scale` 이 그 빈자리를 대신 채웠다.
- **당시 토큰 게이트의 구멍** — 전용 정적 검사는 라이트↔다크 패리티와 라이트→CSS 방출을
  보는데 **JSON 키 → 매핑 존재**는 아무도 안 본다. 그래서 위 고아가 조용히 살았다.

**조치**(착지): 선택자 목록을 **「작은 조작 상자」**(`scale(.97)` 유지 10건)와 **「컨테이너를
채우는 표면」**(4건)으로 가르고, 후자는 hover 가 이미 쓰는 **배경 축**의 다음 단으로 눌림을
말한다. `transform-origin:left` 로 옮기는 안은 기각 — 폭이 여전히 변해 열 정렬 이탈이 남는다.
`translateY(1px)` 도 표에서는 이웃 행과 1px 겹쳐 보여 택하지 않는다.

| 표면 | 눌림 색 | 출처 |
|---|---|---|
| `.job-grp-head` · `.ctx-menu button` | `var(--n-pressed)` | 팔레트에 있던 hover 램프의 다음 단 |
| `.mir-row.miss td` | `var(--fb-missing-press)` | **고아 토큰 회수** — `gen` 매핑 3줄 추가로 방출 |
| `.mir-row.miss.ackd td` | `color-mix(--fb-ack-bg 88%, --a-ink)` | 대칭 토큰 부재 → **파생**(신설 아님을 주석에 남김) |
| `.jobtb tbody tr` | `color-mix(--a-sel 70%, transparent)` | hover 40% ↔ 선택 100% **사이** |
| `.jobtb tbody tr.on` | `color-mix(--a-sel 88%, --a-ink)` | 선택 행은 이미 100% 라 잉크를 얹는다 |

**색은 소유자 파일이 낸다**(`job.css`·`jobdata.css` 의 hover 규칙 옆), **지속시간만 `base.css`
모션 층**이 소유한다. 색을 공용 층으로 끌어오면 §2.1 과 같은 모양이 된다 — 공용 규칙이 곁의
규칙을 덮고, 되읽을 자리가 소유자에서 멀어진다.

**부수 소득**: `reduced-motion` 에서 이제 **전이만** 끈다. 종전엔 이 표면들의 눌림이 통째로
사라져 모션을 끈 사람에게 눌림 피드백이 **0** 이었다 — 그 강등이 이 결함의 로컬 미재현 원인
이기도 했다(개발 기기가 거기 살았다). 색은 이동이 아니라 상태 표지이므로 즉시 남는다.

**당시 검사가 이 결함을 고정시켰다.** 정적 눌림 존재 검사가 8개 선택자 **전부**에
눌림 표지가 있어야 한다고 단언했다 — 규칙의 **존재**는 강제하는데 **기하 결과**는 못 본다.
§2.1 의 그물이 「DOM 이 내는 class 에 규칙이 없다」를 잡았다면, 없던 그물은 **「규칙이 있는데 그
결과가 틀렸다」** 쪽이다.

**검사 복구**(착지) — 셋으로 갈랐다:

| 층 | 파일 | 맡는 것 |
|---|---|---|
| 정적(존재, 당시) | 퇴역한 눌림 존재 census | 두 갈래 각각에 표지 + 강등이 있는지. `.job-item` 부활 금지 |
| 정적(갈림) | `tests/test_web_press_geometry.py` 앞부분 | `scale` 목록이 **선언과 정확히 같은지** — 새 표면이 결정 없이 평면으로 늘면 멈춘다. 죽은 선택자 산출자 0건 재확인 |
| 실렌더(결과) | `tests/test_web_press_geometry.py` + `tests/_press_probe.py` | 실 CSS 를 링크한 최소 문서에서 `:active` **유지 중** 좌변 이탈 ≤ 1px. `reduced_motion` 두 값 강제로 양성·음성 대조 |

곁들여 **주석이 규칙으로 세어지던 것을 고쳤다** — 계약 문서화를 위해 주석에 죽은 선택자 이름을
일부러 남기므로, substring 단언 전에 산문을 걷는다(`_web_css.strip_comments`). 그리고
당시 인접 정적 검사(`:active:not(:disabled){transform:scale(`)를 **두 사실로
갈랐다** — 그 한 덩어리는 선택자 목록의 **꼬리가 무엇인가**에 검사를 묶어, 규칙이 그대로 있는데
목록이 갈리자 빨간불이 났다.

**전제를 시끄럽게 확인한다.** CI 에 `Press-geometry browser precondition` 단계를 세워 설치 Chrome
부재를 테스트 안쪽 오류가 아니라 한 줄로 말하게 하고, 세 옵트아웃 변수를 CI 에서 **걷고** 돌린다
(`tests/repo_contract/test_quality_workflow.py` 가 그 형상을 진다).

**후속 규칙 — 개발 기기가 모션을 꺼 두면 모션 층은 검사된 적이 없다.** 로컬 미재현은 우연이
아니라 구조다: 이 기기는 `SPI_GETCLIENTAREAANIMATION=False` 라 Chromium 이
`prefers-reduced-motion: reduce` 를 보고하고 `base.css:135-137` 이 `transform:none` 으로 끈다.
즉 #194 모션 토큰 층 전체(`:active` 눌림 · `@starting-style` 팝 · 모달 `scale`)가 이 기기에서
**한 번도 돈 적이 없다** — 101 하니스 스크린샷·selftest·눈검증이 전부 「모션 없는 세계」의
산출이었다. 계측 층의 조용한 오류 그 자체라, 프로브를 그냥 붙이면 **영영 초록**이다. 게이트는
`prefers-reduced-motion` 을 **명시로 강제**하고 두 값으로 각각 재서 음성 대조를 먼저 세운다.
실앱(WebView2)은 OS 설정을 따르므로 강제의 매체는 **Playwright + 설치 Chrome 의 `reduced_motion`
컨텍스트**다 — 테스트가 사용자 OS 설정을 변이하지 않는다는 규율이 그 선택의 근거다.

### ~~2.12 삭제 문안이 「휴지통」을 약속한다 — 어휘가 가리킬 것은 undo 다 (사용자 확정 2026-07-30)~~ — **조치완료**(#353 · `6f4c646`)

③에서 승계(§5.2 가 3택으로 들고 있던 항목). 원장이 *"열 수 없는 장소를 약속한다"* 고 적은 것을
실측으로 굳히고 3택을 닫는다.

**실측 — 보존은 참이고 도달 경로가 없다.**

| | 실재 |
|---|---|
| 백엔드 30일 | **참** — 작업 `.trash`(`job.py:1091` · `TRASH_RETENTION_DAYS=30`) · 템플릿 매체 루트 `.trash`(`screen_template.py:351-361`, 같은 30일 컷오프를 손으로 재선언). 정리는 **다음 삭제 때** 지연 수행이라 마지막 삭제분은 기간이 지나도 남는다 |
| 앱의 도달 경로 | `UndoToast` **10초** + **프로세스 메모리 슬롯 1개**(`undo_toast.js:20` — 새 삭제가 이전 콜백을 덮는다) |
| 조회 | `list_trashed` **부재**. 파일명이 `{ts}-{uuid}-{원이름}` 이라 손으로 되돌리려면 접두어를 자르고, **템플릿은 그룹 지정이 안 돌아온다** — 삭제 직후 reconcile 이 지정을 지우고 슬롯이 유일한 생존 기록이었다(`screen_template.py:377`) |

**코드가 이미 알고 있었다** — `undo_toast.js:2`: *"실제 파일은 백엔드 .trash에 30일 남으므로 앱
종료가 곧 즉시 영구 삭제는 아니다"*. 「보존은 있고 표면은 없다」가 **주석에만** 살아 있다.

**진단 — 기능 부족이 아니라 어휘가 다른 물건을 가리킨다.** OS 「휴지통」이 약속하는 것은 셋이다:
열어본다 · 골라 복원한다 · 비운다. 우리 것은 **셋 다 없고** 있는 것은 「직전 1건 · 10초 · 이
세션에서만」이다. 그건 휴지통이 아니라 **undo** 다.

**판정 = A′(표면은 앱이 줄 수 있는 것만 말한다).** 어휘를 **6자리**에서 내린다 — 4자리로 세면
실패 경로가 빠지고, 그 둘이 정확히 「없는 장소」를 가장 크게 말하는 자리다.

| # | 자리 | 지금 | 바뀐 뒤 |
|---|---|---|---|
| 1 | 작업 토스트(`library.js:540`) | 「작업 '…' 을(를) **휴지통으로 옮겼습니다**.」 | 「작업 '…' 을(를) **삭제했습니다**.」 |
| 2 | 템플릿 토스트(`editor.js:579`) | 「템플릿 '…' 을(를) **휴지통으로 옮겼습니다**.」 | 「템플릿 '…' 을(를) **삭제했습니다**.」 |
| 3 | 템플릿 결과줄(`screen_template.py:365`) | 「템플릿을 휴지통으로 옮겼습니다: …」 | **삭제** — 토스트와 같은 말을 두 번 한다(§2.3 0건 note 선례). **되돌리기 어포던스를 든 쪽이 이긴다** |
| 4 | 확인 다이얼로그 `confirmLabel`(`library.js:534`) | 「휴지통으로 이동」 | 「삭제」 |
| 5 | 작업 복원 실패(`job.py:1114`) | 「복원할 작업이 **휴지통에** 없습니다.」 | 「되돌릴 작업 파일을 찾을 수 없습니다.」 |
| 6 | 템플릿 복원 실패(`screen_template.py:391`) | 「복원할 템플릿이 **휴지통에** 없습니다.」 | 「되돌릴 템플릿 파일을 찾을 수 없습니다.」 |

버튼 「되돌리기」(`index.html:515`)는 **무변경** — 이 어휘가 처음부터 맞았고 나머지 여섯이 그것과
어긋나 있었다.

**30일은 표면에 쓰지 않는다**(사용자 확정). 「숨기지 않는다」의 대상은 **사용자가 행동으로 바꿀
수 있는 사실**이다. 앱에서 도달 불가한 보존을 알리면 반드시 「그럼 어디서 여나」를 낳고 그 답이
없으므로 **원 결함이 다른 문장으로 재발한다.** 따라서 A′ 와 A(어휘 완전 제거)의 차이는 문안이
아니라 **백엔드 30일의 처분**이다 — A는 그것을 은연중 죽은 코드로 만들고 A′는 **선언된 미완**으로
남긴다.

**「삭제했습니다」가 반대 방향 거짓이 아닌 근거**: 그 문장은 참이고 **영구 소실을 주장하지
않는다**. 적극적 오약속(「휴지통」)과 전부 말하지 않는 것은 다르며, confirm-or-alarm 이 금지하는
것은 앞쪽이다.

**기각 — 되돌리기 슬롯 영속화(원장 3택의 셋째).** 재시작 뒤 그 1건을 되돌릴 **자리가 없다**:
토스트는 이미 죽었으니 어딘가에 「최근 삭제 복원」이 상주해야 하고, 그것이 서면 그건 1행짜리
휴지통 표면이다. 게다가 「1건이 언제까지 유효한가」(30일? 다음 삭제까지?)를 새로 발명해야 하는데
휴지통 표면은 그 답을 목록으로 갖는다. **휴지통보다 싸지 않고 더 애매하다.**

**휴지통 표면(원장 3택의 둘째)은 별건으로 승격한다** — 짓는다/안 짓는다는 새 기능 결정이라 이
라운드 밖이다. 이슈가 지고 갈 근거 둘을 여기 남긴다:

- **관용 부등식.** `library.js:524` 가 *"사후 관용이 있으므로 사전 확인을 없앤다"* 로 삭제 확인을
  뺐는데 관용의 실제 크기는 **10초·1슬롯·세션한정**이다. **유지로 확정**(사용자 2026-07-30) —
  삭제는 행 ⋮ 속 항목이라 오클릭이 어렵고 10초는 방금 한 행동을 알아채기에 충분하다는 전제.
  **그 전제가 부등식의 유일한 근거이므로 이슈 본문에 적어 둔다** — 전제가 틀리면 사전 확인
  재도입이 아니라 휴지통이 답이다(사전 확인은 forgiveness 계약을 되깎는다).
- **그룹 소실.** 템플릿을 손으로 `.trash` 에서 되돌리면 파일은 살고 그룹 지정은 안 돌아온다.
  **「파일이 남아 있다」가 「복구된다」와 같지 않은 지점**이다.

**삭제가 상속하는 의무.** 이 조치 뒤 백엔드 30일 보존은 **소비자 0인 코드로 보인다.** 지우지
않는다 — `soft_delete`·`_purge_trash`·템플릿 `.trash` 컷오프에 「도달 표면이 아직 없다(§2.12
판정 · 표면은 별건)」를 주석과 **테스트 이름**으로 선언한다. §2.11 의 `badge.missing_press` 가
정확히 반대 경로를 걸었다 — 설계가 있었는데 방출이 없어 조용히 고아가 됐고 `scale` 이 그 자리를
대신 채웠다. 조용한 축소가 아니라 **선언된 축소**로 남기는 것이 차이다.

**계약 영향**: 문안은 전부 JS/Python 문자열이라 정적 DOM 계약 밖이고 `#undoToast*` id 는 존치다.
단 `test_webapp_template.py:298` 이 자리 6 의 문자열을 **사본으로 단언**하므로 같은 변경에 갱신한다
(§2.10 「사본」 선례).

### ~~2.13 확인 2면을 한 면으로 — 필드축 ack 폐기와 1 pane 확인 면 (사용자 확정 2026-07-30)~~ — **조치완료**(#364 · `59d1d8a`)

③에서 승계(§5.4 형상 + §5.2 「확인 2면 재편」 행). §2.10 이 어휘·지목을 국소로 내려보내고
(#335) 남긴 것이 형상이었다. **원장이 적어 둔 형상(2 pane)은 기각되고 더 좁은 답으로 닫힌다.**

**실측이 원장 기술을 세 곳에서 정정한다.** 판정의 근거가 은유 위에 서 있었다.

| 원장이 적은 것 | 실측 |
|---|---|
| *"골격 비용은 이미 지불돼 있다 — 펼침 면이 벌써 `sheet-duo` 2 pane"* | 기법은 실재하고 싸다(`job.js:538-541` 이 `moves:[{id,slotId}]` 목록). 그러나 **2 pane 은 이미 둘 다 점유**됐다 — pane 1 = 거울, pane 2 = `#jobRestate`. 그리고 `renderRestate`(`job.js:900`)의 실제 산출은 주석이 부르는 「게이트 재진술」이 아니라 **파일 이름 계획**이다 |
| *"거울은 N행을 **접은** 것, 드로어는 한 행을 **편** 것"* | 접기·펴기가 아니다. **두 표의 행이 이미 똑같이 필드**이고(`mirrorRow` `job.js:866` ↔ `previewRows` `:644`) 다른 것은 값 열 하나다. 드로어가 거울의 CSS 관용구를 이미 빌려 쓰는 것(`mir-row`·`mir-name`·`mir-val`)이 방증 |
| §2.10 C3 *"같아 보인다"* | **문자 그대로 같다.** `_field_value_display`(`screen_job.py:1022-1027`)의 `filled` 값은 `vals[0]` = **첫 레코드**이고 드로어 `pos 0` 도 첫 레코드다. 값이 전부 같은 필드는 두 면에 **동일 문자열**, 갈리는 필드는 거울만 `(표본 · 외 K개 값)` 을 덧붙인다 |

**C3 은 인지 착오가 아니라 표면의 사실 진술이었다.** #335 의 어휘·지목은 같은 것을 다르게
부른 것이고 원인은 형상이다. 「접은 것/편 것」이라는 **은유가 맞아 보여서 행을 세어 보지
않았다** — 선언은 살고 결과는 죽는 그 결함류의 또 한 표본이다.

#### 형상 — 2 pane 도 아니고 열 추가도 아니다. **1 pane** 이다

세 후보를 세 축으로 재면 순위가 갈린다(시연: `docs/u2-mockups/confirm-surface-compare.html`).

| | C2 골대 | C3 값 중복 | 렌더러 착지 |
|---|---|---|---|
| 2 pane 병치(원장 안) | 닫힘 | 확인 면 안에 남음(필드명 2벌 + 첫 건 값이 좌우로 겹침) | 좋음 — 우 pane 이 그림 |
| 한 표 + 「이 건」 열 | 닫힘 | 집합 열을 **분산축**으로 좁히면 면 안은 0 | **나쁨** — 표 칸에 문서 그림을 못 넣는다 |
| **1 pane**(확정) | 닫힘 | **완전 소멸** | **가장 좋음** — 면 전체가 그림 자리 |

**집합축을 pane 으로 짓지 않는 근거는 「행동을 바꾸지 않는 정보」다**(사용자 판정). §2.12 에서
30일 보관을 표면에서 뺀 것과 같은 잣대를 대면 집합축의 실질은 **둘**만 남는다:

| 집합축이 말하는 것 | 사용자가 할 행동 | 판정 |
|---|---|---|
| 「이 필드는 건마다 다르다」·「전 건 동일」 | 없다 — 다른 것도 같은 것도 정상이다 | **탈락** |
| 「N건에서 빈 값」 | 승인 전에 본다 | 통과 → **표지** |
| 「이름 계획 12건」 | 겹치면 규칙을 고친다(§10.12 판정 K 의 건수 증거) | 통과 → **한 줄** |

**표지 하나와 줄 하나에는 pane 이 필요 없다.** 「분산축 열」안은 탈락한 두 항목에 자리를 지어
준 것이었으므로 함께 기각한다.

**곁들여 「빈 값 있는 건만 보기」를 신설한다.** 「12건 중 2건이 비었다」를 알아도 **어느 2건인지**
아무도 말하지 않아 12건을 다 넘겨야 했다. `‹ ›` 이동을 그 건들로 한정한다 — 선례는 결과 존의
「실패한 건만 선택」(`index.html:277`, §10.10 판정 F: 선택만 바꾸고 생성하지 않는다).
**훑기 가속의 실제 기제는 표지가 아니라 이 한정이다.**

#### 필드축 ack 게이트를 폐기하고 승인 하나로 간다

**근거는 렌더러 부채가 아니라 위험이다**(사용자 지적을 실측이 강화한다). 렌더러 위에서 필드별
클릭을 구현하려면 문서 그림에 **필드 히트박스 좌표 계약**이 생긴다. 그런데 §5.4 가 렌더러에
이미 *"부분 충실도 렌더는 조용히 틀린 그림"* 이라는 의무를 지웠으므로 근사 구간이 존재하고,
거기서 히트박스가 어긋나면 사용자는 **엉뚱한 필드를 확인**한다. 확인 게이트를 그 게이트가
막으려던 것으로 오염시키는 길이다.

**ack 의 정체 실측 — 「봤다」가 아니라 「표식을 박고 진행하겠다」다.**

```python
MISSING_MARKER = "〘미입력·{field}〙"                    # core/job.py:46
def _run_marker(self, indices):                        # screen_job.py:643
    if self.vm.unmet_blanks(indices) or not self.vm.blank_fields(indices):
        return ""
    return MISSING_MARKER
```

빈 값이 있으면 ⑴ 게이트가 막고 ⑵ 확인하면 열리고 ⑶ **문서에 표식이 박힌다**. CLI 도 같은 말을
한다(`cli.py:481`: *"표식을 넣고 진행"*). 즉 ack 는 열람 확인이 아니라 **표식 삽입 동의**이고,
CLAUDE.md 의 *"빈 값은 빈칸으로 새지 않고 표식으로 남는다"* 를 실행하는 손잡이다. **그래서 승인
1번이 그 동의를 겸할 수 있다** — 강제 상호작용의 **횟수**만 줄고 성질은 같다.

**조건 — 승인 지문에 빈 값 집합이 들어가야 한다. 같은 슬라이스에서.**

승인은 규칙 지문 기반이라 **한 번 완주하면 영구히 조용해진다**(§10.12 판정 N). 그대로 대체하면
다음 달 새 데이터에서 빈 값이 새로 생겨도 게이트가 서지 않고 **표식이 박힌 문서가 조용히
생성된다** — 문서를 열면 표식이 보이므로 완전한 침묵은 아니지만 **생성 시점에 아무도 안 말하고**,
제출한 뒤에 아는 순서가 된다. 그래서 폐기와 보정은 **한 슬라이스**다(폐기만 먼저 착지하면 그
사이에 열리는 구멍이 이것이다).

고칠 자리는 이미 있다 — `_review_scope_key`(`screen_job.py:652`)가 스스로 적어 뒀다:
*"표식 상태도 승인의 일부다(4R P2)"*. 지금 그 성분이 **이진값**(표식 있음/없음)이므로 **빈 값
필드 집합의 해시로 승격**시킨다. 새 기제가 아니라 있는 성분의 해상도를 올리는 것이다. 링1 에는
위험종 하나가 서고(`ReviewRequirement.risk_class`), 빈 값 집합은 선택에 따라 달라지므로 반드시
**`selection_bound=True`** 다 — `presentation` 이 규칙에만 결속되는 것과 대조(`review_state.py:101`).

| | |
|---|---|
| **죽는다** | `run_state`: `acknowledge`(:575)·`unacknowledge`(:579)·`reset_acks`(:587)·`acked_count`(:591)·`unmet_blanks`(:599)·`FieldState.acknowledged`(:74) · action registry `ack_field`·`unack_field`(:171-172) · `screen_job._do_ack_field`(:2065)·`_do_unack_field`(:2071)·`guard["ack_count"]`(:1132) · 거울의 클릭형 행(`role="button"`·`aria-pressed`)과 **UD-19 재클릭 토글** · 게이트 서열의 「본문 확인」 한 단 · `app.py:1362` 셀프테스트 `confirm_dispatch`(`ack_field` 디스패치를 단언한다) |
| **산다** | **`MISSING_MARKER` 와 문서 표식** — 조용한 통과를 막는 최후 장치라 손대지 않는다 · 「N건 중 K건이 빈 값」 **표지**(정보로 남고 클릭 표적만 사라진다) · 승인·`approval_key`·`_review_scope_key` |
| **재정의** | `_run_marker` — 「unmet 이 없고 빈 값이 있으면」에서 **「빈 값이 있으면」** 으로 단순해진다. 생성·미리보기·승인의 **단일 술어**라는 성질은 그대로 |

**삭제가 상속하는 의무 셋.**

- `acked_count` 의 소비자는 **데이터 전환 손실 열거**(F1 · §10.7.3 감사 — *"세워 둔 확인이 있으면
  가드 문안이 그 사실을 말해야 한다"*)다. ack 가 없으면 그 손실 항목이 사라지므로 **가드 문안에서
  ack 성분을 걷는다.** 안 걷으면 가드가 **존재하지 않는 것을 잃는다고** 말한다.
- **§2.10 의 필드축 어휘가 사라진다.** 「확인」을 필드축 단독으로 준 것이 C1 의 조치였는데 그 축이
  없어지므로 표면에는 「승인」만 남는다. **되깎기가 아니다** — 남는 쪽이 「승인」이라 #335 의 방향은
  유지되고, 거울 캡션 「본문 확인」과 안내 문안이 함께 바뀐다.
- **§3 말미의 게이트 서열 참고 블록**은 지금 코드에 대해 참이므로 그대로 두고, 이 폐기가 착지하면
  ack 단을 걷는다. 미착지 상태에서 미리 고치면 문서가 현재 동작을 오도한다. — **이행됨**: 착지
  (`59d1d8a`)와 함께 그 블록이 갱신됐다(ack 단 삭제 · 빈 값은 `blank_set` 위험종).

#### 인라인 거울 존은 존치 근거를 잃는다 → 표 없는 한 줄

§10.6 결정 3 이 존을 지킨 근거는 *"거울은 `ack_field` 게이트를 푸는 **유일 표면**이라 숨기면
차단된 사람의 출구가 사라진다"* 였다. **ack 가 죽으면 그 근거가 함께 죽는다.** 남은 기능은 값
훑기인데 그 값이 대표값이라 **부정확한 훑기**다 — 정확한 훑기는 원래 확인 면에서만 가능했다.

그래서 존을 한 줄로 줄인다: 「본문 확인 — 빈 값 2필드(담당자·개찰장소) · 이름 12건 〔생성 값
미리보기 ⤢〕」. **값을 말하는 표면이 확인 면 하나뿐이 되어 C3 이 완전히 닫힌다** — 2 pane 안도
열 추가 안도 인라인 존을 값 표면으로 남기므로 여기까지 가지 못했다.

빈 값 행은 확인 면에서 **무엇이 문서에 박히는지를 그 자리에서 말한다**(「(빈 값) → 〘미입력·
담당자〙 로 박힙니다 · 12건 중 2건」). 종전 「클릭=확인」은 동의의 **결과**를 말하지 않았다 —
무엇에 동의하는지 모르고 눌렀다. 승인 1번으로 대체되면서 동의의 내용이 오히려 명시된다.

#### 계약 비용과 범위

`#previewModal` 은 죽는 대신 **시트로 승격**한다(렌더러가 올 자리라 680px 모달로는 좁다) —
id 변경이므로 **정적 DOM 계약 선행 갱신** · selftest 프로브 재작성 · `UI_CONTRACT.md` 화면별 계약.
`preview_move`·`preview_approve` 는 액션 유지·소비 표면 교체다. `#jobConfirmSheet` 의 `sheet-duo`
2 pane 은 사망하고 `#jobRestate` 는 확인 면의 이름 한 줄로 이주한다. 우 pane 을 **공용 「레코드
pane」 계약**으로 세울지는 착수 시 판단(지금 `.wb-preview`와 드로어가 CSS·DOM 남남이다).

**범위**: §5.3 과 같은 방식이다 — **판정은 U2 가 소유**하고 **구현은 U2 밖 슬라이스**다. 이산
조치(ack 5메서드·2액션 폐기 · `blank_set` 위험종 신설 · `_run_marker` 재정의 · 인라인 존 축소 ·
확인 면 시트 승격 · 「빈 값 있는 건만 보기」 · 데이터 전환 가드 문안)는 **GitHub 이슈**가 진다.
**폐기와 `blank_set` 보정은 쪼개지 않는다** — 위 조건 문단이 그 이유다.

### ~~2.14 저장 착지가 뒤로 간다 — 신규만 매핑 탭 (사용자 확정 2026-07-30)~~ — **조치완료**(#354 · `da474fe`)

③에서 승계(§5.2 「저장 후 2단계 착지」). 원장이 *"버그가 아니라 의도된 코드"* 라고 적은 것은
참이다. 틀린 것은 **의도의 출처**다 — 그 분기의 자기 설명은 판정이 아니라 *"기존 전환점을
**유지**한다"* 는 **승계**이고, 되깎기에 걸어 둔 조건은 **이미 충족돼 있었다.**

**실측 ⑴ — 점프는 hwpx 전용이다. 매체가 다른 규칙을 따르는 것이 아니라 한쪽에서만 안 보인다.**
신규 마법사에서 「작업 저장」은 **마지막 단계에만** 선다(`editor.js:946` — `last` 일 때만 primary
가 save 로 바뀐다).

| 매체 | 섹션 | 저장이 일어나는 자리 | 착지 | |
|---|---|---|---|---|
| hwpx | `template·binding·filename` | **filename**(마지막) | `binding` | **3 → 2, 뒤로** |
| txt | `template·binding` | **binding**(마지막) | `binding` | **이미 제자리** |

**실측 ⑵ — 되깎기 조건은 충족됐다.** 원장이 건 조건은 *"그 결함 재발 경로를 먼저 막아야 한다"*
였고, 그 결함은 구판 `_reset()` 이 사용자를 빈 0단계 마법사에 두고 **그 리셋 push 가 `#save-msg`
를 지운** 것이다. 지금 착지는 저장본 재로드(`_restore_from(..., emit_push=False)`)라 빈 마법사가
아니고, 성공 재진술은 push 경합에 안 걸리는 **notice(ok) 채널**이 진다(`editor.js:1268` 이 그
사실을 적어 뒀다). `landing_section` 값은 **그 두 방어의 어느 쪽에도 참여하지 않는다** — 이 조치는
되깎기가 아니라 **같은 방어 안에서 탭만 제자리로 돌리는 것**이다.

**조치**: `screen_editor.py:1929` 의 분기를 지우고 `landing_section = self.section` 무조건.
신규·편집이 한 규칙을 쓰고 hwpx·txt 가 같은 결과를 낸다. 결정 40 「저장 제자리」가 **두 경로
모두에서 처음으로 참이 된다.**

**§2.5 가 이 자리에서 닫힌다.** 제자리에 남으면 완결 신호 넷이 **시선이 머문 면에서** 일어난다.

| 신호 | 자리 |
|---|---|
| notice 「작업 '…' 을(를) 저장했습니다」 | 본문 최상단(`editor.js:258`) |
| 버튼 「작업 저장」 → 「변경 저장」(dirty 0 이라 비활성) | **누른 그 자리** |
| 칩 「아직 저장하지 않은 새 작업」 → 「저장됨 · 템플릿 r1 · 연결 r1」 | 헤더(`editor.js:187-194`) |
| 헤더 단계 표지(번호·게이트) → 자유 이동 탭 | 헤더(`stepHeader`) |

지금은 본문이 통째로 매핑 표로 갈리며 이 넷이 **화면 교체에 묻힌다.** 그 위에 방향이 얹힌다 —
3단계에서 2단계로 내려가는 이동은 **「저장이 안 돼 되돌아갔다」로 읽히는 관용구**다. 착지 탭이
곧 `saveStage`(작업명 + 파일 이름 규칙 + 저장 버튼)라는 것도 제자리 편이다: 방금 지은 이름이
그대로 서 있는 면이 완결 화면으로 제일 정직하다.

**기각 — 저장 후 편집기 이탈.** 「저장하고 나가기」가 이미 **명시 행동**으로 있다
(`editor.js:1453` 이탈 가드 3택). 저장이 자동으로 나가면 두 행동이 같아지고 **몰입 표면 이탈이
사용자 선언 없이** 일어난다 — 가드가 처분을 고르라고 세운 자리를 미리 소비하는 셈이다.

**검사가 이 자리를 한 번도 안 밟았다 — 조치보다 이쪽이 크다.**
`test_save_lands_in_edit_session_of_saved_job` 이 `section == "binding"` 을 단언하지만, 헬퍼
`_save_named` 는 binding 에서 `set_pattern` 후 바로 저장해 **`filename` 에 간 적이 없다.** 실 UI
가 저장하는 **유일한** 자리를 헤드리스가 지나지 않으므로 그 단언은 **고치든 안 고치든 초록**이다.
「선언은 살고 결과는 죽는다」의 또 한 표본이라, 조치는 **filename 까지 전진한 뒤 저장하는** 경로를
세우는 테스트를 동반한다. 기존 단언은 **지우지 않는다** — 그 흐름에서는 제자리가 곧 binding 이라
여전히 참이고, 두 경로가 **한 규칙의 두 결과**임을 그 쌍이 말한다.

**계약 영향 0**: 페이로드 키·DOM id·액션 무변경이고 `landing_section` 은 컨트롤러 내부 값이다.
드로어 deep-link 착지(`target` 이 탭을 정한다, §10.14.3)는 다른 경로라 무영향 — 그쪽은
`load_job` 이 `context.target` 에서 탭을 뽑는다(`screen_editor.py:1000`).

### ~~2.15 복사 완료 건이 후미로 간다 — 두 순서 중 하나를 지운다 (사용자 확정 2026-07-30)~~ — **조치완료**(#357 · `4592f35`)

③에서 승계(§5.2 「복사 완료 건 후미 이동」). 원장은 **「미처리 우선 큐」 vs 「고정 순서 + 상태
색」** 2택으로 들고 있었다. 실측은 그것이 택일이 아니라 **이미 갈려 있는 두 순서 중 하나를
지우는 문제**임을 보인다.

**⑴ 표면은 목록이 아니라 카드 1장 + 익명의 점 띠다.** `#wbDots` 의 점은 이름이
`title`·`aria-label` 에만 있고(원본 행 번호) 표면에 글자가 없다. 사람은 점을 **자리로 기억**
하므로 재정렬이 곧 그 기억의 파기다.

**⑵ 한 화면에 순서가 둘인데 부제는 그중 하나만 약속한다.**

| 축 | 쓰는 곳 | 순서 |
|---|---|---|
| 사람이 읽는 자리 `position` | 카드 서수 | **고정 사본**(선택 당시 표시순서) |
| 점 띠 `index_map` · 이동 경계 `can_prev`/`can_next` | 순회 | **큐**(미처리 → 처리 후미) |

`screen_workbench.py:366-370` 이 앞쪽 선택의 근거를 적어 뒀다 — *"이 화면의 부제는 「선택 당시
표시순서로 고정된 항목」이라고 말하므로, 사람이 읽는 숫자도 그 고정 순서를 따라야 참이다
(§13-13)"*. 부제(`index.html:438`)가 **고정을 약속하는데 눈에 보이는 띠는 고정이 아니다.**

**⑶ 그 이중성이 이미 결함을 냈고, 조치는 화해였지 해소가 아니었다.** 같은 자리 주석
(`:374-379`, 2R P1): *"복사하면 그 카드가 후미로 가므로 자리는 그대로인데 다음/이전은 달라진다.
하나의 값으로 두 질문에 답하면 그중 하나는 반드시 틀린다 — 실제로 복사 직후 「다음」이 눌리지만
아무 일도 안 하고 「이전」은 비활성이라 그 카드에 갇혔다."* 두 순서를 남긴 채 **값을 둘로
늘렸다.** 이슈 **#290**(「복사 완료 후 이동 오작동」 · **재현 미확보**)이 같은 계열이지만 재현이
없으므로 **원인이라 단정하지 않는다** — 이 조치는 그 증상류의 후보를 하나 없앨 뿐이다.

**⑷ 재정렬이 하려던 일을 다른 셋이 이미 한다.**

| 정렬이 말하던 것 | 이미 말하는 것 |
|---|---|
| 무엇이 끝났나 | 점 색 3태(`current`·`copied`·`uncopied`) + `recheck` gap |
| 어디로 갈까 | `advance_to_next_uncopied`(복사 후 자동 전진 opt-in `#wbAdvance`) |
| 얼마나 남았나 | `uncopied_count` |

**같은 사실을 위치로 한 번 더 말하고, 그 잉여가 자리를 흔든다.**

**⑸ 규율은 이미 서 있고 이건 그 미이행분이다.** `txt_queue.copy()` 주석이 *"조용한 작업점 이동
금지"* 를 못 박아 **작업점은** 안 움직이게 해 놨다. 그런데 **카드가 띠 안에서 움직인다** — 규율의
대상이 커서였고 자리는 빠져 있었다. 저장소 관용구도 재정렬이 아니라 **한정**이다(결과 존 「실패한
건만 선택」 · §2.13 「빈 값 있는 건만 보기」).

**판정 = 고정 순서 + 상태 색.**

| # | 조치 |
|---|---|
| 1 | `display_order()` 를 **고정 사본 순서**로. `_uncopied_order`·`_copied_order` 는 **집합 상태**로 남고(`is_copied`·`uncopied_count` 불변) **순서 지배만 잃는다** |
| 2 | `can_prev`/`can_next` 를 표시 서수로 답한다 — `_queue_pos()` 와 `position` 의 갈림이 소멸하고 **두 순서를 화해시키던 주석 두 덩이가 함께 죽는다.** 그 삭제가 조치가 일했다는 증거다 |
| 3 | `position_of`(미처리 큐 1-기반 순번)의 소비자 재확인 — 페이로드는 이미 안 쓴다(주석이 *"여기 쓸 수 없다"* 고 적었다). 0곳이면 죽이고, 남으면 그 소비자가 **미처리 순번을 정말 원하는지** 재진술한다 |
| 4 | 전진 규칙은 **현행 유지**(`advance_to_next_uncopied` = 가장 이른 미처리) |

**전진 규칙을 그대로 둔 근거와 사정권**(사용자 확정). 지금은 복사분이 가라앉아 「가장 이른
미처리」가 곧 「커서 오른쪽」이라 두 규칙이 **눈으로 구별되지 않는다** — 순서를 고정하는 순간에만
갈리고, 갈리는 지점은 **끝 도달이 아니라 건너뛴 건의 회수 시점**이다.

| 상황 | 현행(가장 이른 미처리) | 대안(현재 다음 → 없으면 랩) |
|---|---|---|
| 건너뜀 없이 순서대로 | 오른쪽 다음 칸 | **동일** |
| 5번 복사 직후 2번이 미처리 | **즉시 2번으로 되돌아감** | 6번 (2번은 나중) |
| 오른쪽 끝까지 훑고 2번만 남음 | 2번 | 랩으로 2번 — **동일** |
| 미처리 0 | 현 위치 유지 | **동일** |

현행을 고른 근거는 규칙이 **하나**라는 것(「빠뜨린 것부터 챙긴다」)과 **점 띠가 고정이라 되돌아간
자리가 보인다**는 것이다. 사정권도 좁다 — 손으로 누르는 `‹ ›`(`.wb-foot-nav`)는 `step()` =
**표시순 이동**이라 이미 별개이고, 이 규칙이 정하는 것은 `#wbAdvance` 가 켜졌을 때의 목적지
하나다.

**사용자가 내주는 것**: 「남은 것이 왼쪽에 모여 있다」는 **위치 신호**. 진행은 색 분포와
`uncopied_count` 수치로 읽는다. 그 대가로 얻는 것은 **부제의 약속이 참이 되는 것**이다.

**계약 영향**: `index_map` 은 키 불변이고 **값의 순서만** 바뀐다(정적 DOM 계약 밖). 갱신은
`tests/test_txt_queue.py`(`display_order`·`copied_tail` 단언 10곳)·`test_webapp_workbench.py`,
그리고 셀프테스트가 점 띠 순서를 단언하면 함께. **핵심 단언은 모델 층이 진다** — 복사 뒤
`display_order()` 가 **불변**임을 세우는 것이 이 판정의 회귀 그물이다.

### 2.16 부트스트래핑 — **narrow 조치완료**(#355 · `29800cc`), 패키지(broad)는 별건 **#351** (사용자 확정 2026-07-30)

③에서 승계(§5.2 「부트스트래핑」). **「짓는다」는 2026-07-29 에 이미 확정**됐고 남은 것은 범위뿐
이었다 — **narrow 확정 · broad 별건 승격**.

**⑴ 지금은 「1개 + 즉시 채택」이다.** `import_template_file`(`app.py:248`)이
`open_file_dialog`(**단일 선택** — `dialogs.py:149`, `OFN_ALLOWMULTISELECT` 없음)로 파일 하나를
받아 복사한 뒤 **편집 세션이 그 사본을 채택**한다. 그래서 호출부가 세션 확인을 먼저 건다
(`editor.js:1074` `confirmNewSessionIfUnsaved`). 템플릿 N개를 넣으려면 **마법사 왕복 N번 + 세션
확인 N번** — 마찰의 실체는 개수가 아니라 **「넣기」와 「쓰기」가 한 동사로 묶인 것**이다.

**⑵ 일괄이 새로 지을 것은 루프와 재진술뿐이다.** 복사 권위 `import_into_library`
(`screen_template.py:180`)가 이미 전부 갖고 있다 — 확장자 매체 라우팅(`.hwpx`→라이브러리 ·
`.txt`→텍스트 레지스트리) · 충돌 `이름 (2).ext` 접미 + 재진술 · 인스턴스 잠금 직렬화 ·
**무잔재**(부분 파일 회수 후 재던짐) · 「그룹 없음」 시작. 「그 문제가 여기서 이미 어떻게
풀리는가」를 세면 **거의 다 풀려 있다.**

**⑶ 자리도 있다.** 관리 표면은 죽은 `tpl` 화면을 승계한 편집기 **「템플릿」 탭 상단 행동 줄**
(`editor.js:362-365`)이고, 빈 상태 문안이 이미 그 자리를 가리킨다.

| # | 조치 | 근거 |
|---|---|---|
| 1 | **「폴더에서 가져오기…」 신설** — `open_folder_dialog` → 직속 `.hwpx`/`.txt` 수집 → `import_into_library` 반복 | 복사 권위 재사용. 매체 라우팅·충돌 규칙을 **다시 짓지 않는다** |
| 2 | **채택하지 않는다** — 세션 무변경이므로 `confirmNewSessionIfUnsaved` 도 걸지 않는다 | N개 중 무엇을 채택할지 답이 없다. 「넣기」와 「쓰기」를 가르면 **편집 중에도 안전한 동사**가 된다 |
| 3 | **고른 뒤 먼저 재진술하고 확정받는다** — 「.hwpx 12건 · .txt 3건 · 나머지 20개는 가져오지 않습니다 · 이름 충돌 2건은 `(2)` 접미로」 | 무엇이 들어오는지 모른 채 20개가 복사되면 안 된다. 충돌 접미는 사후 재진술이 이미 있지만 **일괄에서는 사전 예고가 값이 다르다** |
| 4 | **1단계만** — 하위 폴더를 훑지 않는다 | 조용한 확장 금지. 어차피 평면 루트로 복사돼 트리 구조는 버려지고 **동명 충돌만 늘어난다**. 필요하면 폴더를 다시 고른다 |
| 5 | 그룹은 **「그룹 없음」 그대로** | 아래 |
| 6 | 부분 실패는 **걷어내고 계속하되 사유를 병기**한다 — 「12건 중 10건 등록 · 2건 실패(사유)」 | `import_into_library` 의 무잔재 계약을 건별로 반복. 하나가 죽어 열이 안 들어가면 일괄이 아니다 |

**그룹 자동 배정을 안 들인 근거**(사용자 확정). 「폴더 이름을 그룹으로」를 확정 다이얼로그
체크박스로 넣는 안이 있었으나 **기존 계약을 그대로 둔다** — `import_into_library` 가 「그룹 없음
에서 시작」을 계약으로 적어 뒀고, 일괄이라는 이유로 예외를 내면 **단건과 일괄의 결과가 갈린다**
(§2.4 「일괄판은 단일판과 같은 착지」와 같은 잣대). **내주는 것은 정직하게 적는다**: 12개를 넣은
뒤 묶으려면 목록에 다중 선택이 없어 **항목당 하나씩** 지정해야 한다. 그 노동이 실제로 물리면
답은 자동 추측이 아니라 **목록 다중 선택 + 일괄 그룹 지정**이고, 그것은 별건이다.

**broad 는 별건 승격**(**#351** — 열려 있다. 선행 §5.3 판정 B 는 **#348 → #368 · `18f84c4`** 로
착지해 선행 조건은 풀렸다). 이슈가 지고
갈 근거 셋을 여기 남긴다:

- **선행 = §5.3 판정 B**(라이브러리 루트 상대키). 상대키 없이 패키지를 열면 받는 쪽에서 전 작업이
  「템플릿 연결 안 됨」으로 선다 — `Job.template_path` 가 **남의 PC 절대경로**다.
- **이식 규칙 자체가 새 결정이다** — 판본·규칙 지문·검토 기준선(`reviewed_rules`)을 받는 쪽이
  **승계할지 초기화할지**. 승계하면 **남이 승인한 것을 내가 승인한 것으로 읽고**(§10.12 판정 N 의
  「작업당 1회」가 남의 1회를 가져온다), 초기화하면 받자마자 전원이 승인 요구로 선다. 둘 다
  가능하지만 **조용하면 안 되는 축**이다.
- **표준에서 이 기능의 이름은 Collect/Package 다**(§5.3 웹 표준 표 2행) — 평소엔 참조, 전달 시
  자산을 모아 자기완결 폴더로. 즉 **부트스트래핑의 반대편(내보내기)이 같은 슬라이스에 붙는다.**

**#284 와 갈리는 지점**: 그쪽은 **배포본 동봉 예제 세트**(빈 상태 제안 + 「예제」 그룹 삭제로 통째
제거)이고 이쪽은 **사용자 자신의 자산**이다. 표면도 다르다 — 예제는 빈 상태 제안, 일괄 등록은
상시 행동 줄. 다만 **둘 다 「라이브러리를 채우는 경로」라 빈 상태 문안이 둘을 함께 말해야 한다**
(지금은 *"'가져오기…'로 추가하세요"* 하나뿐이다).

**계약 영향**: 새 **직접 브리지 메서드 1개**(`import_templates_folder`) — action registry 밖이므로
payload 검증을 메서드 본문에 쓰고 `docs/UI_CONTRACT.md` 의 직접 메서드 목록을 **같은 변경에**
갱신한다. DOM 은 행동 줄 버튼 1개 추가라 정적 DOM 계약도 함께 간다.

**narrow 착지가 낳은 잔여 하나**: 3행 재진술의 **목적지 수치 결속**이 별건 **#365** 로 갈렸다
(#339 리뷰 4라운드 분리분 — 근거와 편입처는 §1.1 「착지가 낳은 별건」이 소유한다. 여기서 다시
서술하지 않는다).

### ~~2.17 취소·저장 문법 — 출구는 하나로 두고, 어긋난 것은 dirty 규칙이었다 (사용자 확정 2026-07-30)~~ — **조치완료**(#354 · `da474fe`, §2.14 와 같은 PR)

③에서 승계(§5.2 「취소·저장 문법」). 2택은 **오버레이 강조 유지 vs 취소/저장 2버튼**이었고
**전자로 확정**한다. 다만 실측은 제안이 겨눈 자리와 **실제로 어긋난 자리가 다르다**는 것을 보인다.

**⑴ 두 모드의 현재 문법.**

| | 푸터 | 나가는 길 |
|---|---|---|
| 신규(마법사) | `[취소] [◀뒤로] … [다음 ▶ / 작업 저장]` | 「취소」=`cancel-new`(세션 폐기) |
| 편집(탭) | `[변경 버리기 — dirty 때만] … [변경 저장 — 상시·비활성]` | **좌상단 「← 원래 업무로 돌아가기」뿐** + 3택 가드 |

**⑵ 2버튼 문법을 기각하는 근거 — 출구는 이미 하나로 모여 있고, 그 하나가 3택을 준다.**
`leaveTo`(`editor.js:1430`)가 스스로 적어 뒀다: *"편집기를 나가는 **단일 출구**(§10.13 판정 N) —
back·문맥 복귀·다른 화면의 프로그램적 이동이 전부 여기로 모인다."* dirty 면 **저장하고 나가기 /
버리고 나가기 / 머무르기**를 묻는다. 여기에 푸터 「취소」를 더하면 갈림은 둘뿐인데 **둘 다 나쁘다**:

- 「취소」 = **버리고 나가기** → 출구가 둘이 되고, 그 버튼이 **3택이 주는 「저장하고 나가기」를
  선점**한다. 파괴 처분을 확인 없이 한 번에 실행하는 길을 새로 내는 것이다.
- 「취소」 = **변경 버리기**(머묾) → 이름만 둘인 같은 버튼. 어휘 충돌은 §2.10 이 방금 정리한 축이다.

편집기는 **몰입 전체 화면**이지 다이얼로그가 아니므로 취소/저장 쌍은 애초에 이 표면의 문법이
아니다. 신규의 「취소」가 정당한 것은 그것이 **세션 폐기**라는 다른 일이기 때문이다 — 이름이
다른 것이 옳다.

**⑶ 실제로 어긋난 것 — 같은 축을 두 버튼이 다르게 말한다.** 「변경 저장」은 **상시 표시 +
`dirty` 없으면 비활성**인데(§2.4 · #333 에서 그렇게 착지시켰다) 「변경 버리기」는 **아예
나타났다 사라진다**(`editor.js:932`). 자리가 흔들리고, 사용자는 한 축(`dirty`)에서 **두 규칙**을
배운다. 원 제안이 「버튼이 없다」고 느낀 지점의 실체가 이것이다 — 문법이 아니라 **가시성 규칙**.

**조치**: 「변경 버리기」를 **저장과 같은 규칙**으로 — 상시 표시 + `dirty` 없으면 비활성. 현행
주석의 논거(*"손댄 것이 없으면 버릴 것도 없다"*)는 참이지만 **그 사실을 비활성이 이미 말한다**.
버튼 하나가 나타나며 푸터 배치를 미는 것보다, 같은 자리에서 색이 죽는 편이 규칙을 가르친다.

**곁들여 확인할 것**: 저장 게이트는 `s.dirty || pendingFieldEdit` **합성**을 쓴다(`editor.js:940`
— 확정 전 편집이 삼켜지던 자리). 버리기도 **같은 술어**를 써야 한다 — 안 그러면 「고치는 중인
편집」을 저장은 받아 주는데 버리기는 못 버린다.

**삭제가 상속하는 의무 — 프로브 하나가 조용히 무의미해진다.** 셀프테스트가
`out.dirty_discard_shown = !!document.querySelector('#editor-foot [data-act="discard-patch"]')`
(`app.py:1630`)로 **존재**를 단언한다. 그 단언이 지키던 것은 *"「저장됨」이라 말하면서 버릴 길도
없던 자리(3R P2)"* 인데, 상시 표시가 되면 **무엇을 밀어 넣어도 참**이 된다. 같은 변경에서
**비활성 판정으로 승격**하고 `dirty`/clean **두 값으로 대조**한다 — 한 값만 재는 프로브는
아무것도 못 재는 프로브다(§2.11 모션 층과 같은 계열). 이 항목에서 제일 조심할 자리다.

**계약 영향**: DOM id 신설 0. `discard_patch` 액션·payload 무변경 — 현재 어휘·payload 경계는
`tests/repo_contract/test_dispatch_payload_contract.py`, 화면 동작은
`tests/js/n06_editor.test.js`가 잇는다.

### ~~2.18 결과 처분 — 지문 성분별로 가른다 (사용자 확정 2026-07-30)~~ — **조치완료**(#363 · `141fbf1`)

③에서 승계(§5.2 「데이터·문서 재선택 시 결과 처분」). 원장은 *"현행은 삭제가 아니라 강등(§10.10
판정 G). **「의도적」이라는 사실이 「옳다」는 뜻은 아니다**"* 라고 열어 뒀다. 실측은 판정 G 가
**틀린 것이 아니라 너무 넓게 적용됐다**는 것을 보인다.

**⑴ 링1 은 이미 지운다 — 남기는 것은 링2 뿐이다.**

| 자리 | 하는 일 |
|---|---|
| `screen_job.py:1686` | `self._last_generated = None` — *"실행 증거는 세션 스코프 — 전환 시 소멸(§19.10)"* |
| `:2062` | *"완주 집합의 인덱스는 이전 데이터 좌표 — 교체 시 무효"* |
| `:1452` · `:2102` | `_last_failed = []` — *"실패 index 는 이 레코드 집합에서만 뜻이 있다"* · *"파일 마운트와 같은 수명(§10.10 판정 F)"* |

**웹의 `RESULT` 만 강등된 사본으로 남는다.** 링2 가 링1 이 죽인 것을 화면에 계속 그리는 형상이다.

**⑵ 세션 계약이 이미 그렇게 말한다.** `CLAUDE.md`: *"마운트된 데이터·선택·필터는 `JobController`
세션 소유이고 작업 전환에서 생존한다. **잃는 것은 실행 증거뿐**."* 이 판정은 새 설계가 아니라
**적혀 있는 계약의 미이행분 회수**다.

**⑶ 판정 G 의 근거는 다른 축에서 나왔다.** 「지우지 않는다」의 실제 논거는 `job.js:104` 이
적어 뒀듯 *"지우면 「실패한 N건만 선택」이 자기 결과를 없애 무엇을 다시 만드는지 볼 수 없고
(**선택 변경이 곧 지문 변경**)"* 이다 — **선택 축의 자기모순**을 막는 논거다. 세션 지문
5성분(작업 · 데이터 · 저장 폴더 · 선택 · 규칙) 중 그 논거가 사는 곳은 **선택·규칙**이지 작업·
데이터가 아니다. 한 논거로 다섯 성분을 덮은 것이 과적용이다.

**판정 — 성분별 2분기.**

| 지문 성분 | 처분 | 근거 |
|---|---|---|
| **작업 전환 · 데이터 교체** | **초기화** | 링1 이 증거를 죽였고 계약이 「잃는 것은 실행 증거뿐」이라 말한다. 지금도 작업이 갈리면 행동은 전부 걷혀(`foreign` — 「실패한 N건만 선택」·「이름 바꾸기」 hidden) **증거만 남은 껍데기**다 |
| 선택 · 규칙 · 저장 폴더 | **강등 유지** | 판정 G 의 자기모순 논거가 정확히 여기 산다. 「실패한 N건만 선택」이 자기 근거를 지우면 안 된다 |

**조용한 손실이 아닌 근거**: 소멸은 **명시 행동**(데이터·작업을 바꿨다)의 직접 결과이고 **전면
가시성**(그 자리에서 존이 닫힌다)이다 — §2.9 계열이지 조용한 추측이 아니다. **가드 문안에는
넣지 않는다**: 데이터 재겨눔 가드는 **무장(손으로 고른 선택)일 때만** 서는데 결과 소멸은 무장과
무관하게 일어나므로, 거기 한 줄 넣으면 **가드가 뜬 경우에만 알리는 반쪽 고지**가 된다.

**⑷ 도달성은 실행 기록 한 줄이 진다**(사용자 확정 — 「그냥 내준다」에서 갈렸다). 저장 폴더는
**작업 파생**(`job.template_path/Results`, `:1717`)이라 작업을 되고르면 경로가 복원되고 데이터만
바꾼 경우엔 애초에 안 바뀐다. **잃는 경우는 하나뿐** — 그 런의 저장 폴더를 **손으로 골랐을 때**
(작업 전환이 `out_dir` 을 작업 기본값으로 덮고, 그 경로의 유일한 보관처가 결과 존이었다). 그래서
초기화하는 순간 `#jobRunLog` 에 **퇴장 한 줄**을 적는다: 「'발주요청서' 12건 생성(2건 실패) —
`C:\…\Results`」.

**로그의 정체를 다시 못 박는다** — 지금 주석은 *"여기 남는 것은 이 화면의 **비-결과 사건**이다
(결과 사건은 3태 구획이 가져갔다, §10.10 판정 D)"* 이다. 그 경계는 **유지된다**: 로그가 받는 것은
결과의 사본이 아니라 **「결과가 세션에서 물러났다」는 사건**과 그때의 경로다. 결과 **자체**(파일
목록·실패 사유·증거)는 여전히 3태 구획만 소유하고, 그것들은 초기화와 함께 사라진다 — 실패 사유를
붙들고 싶으면 **데이터를 바꾸기 전에** 본다.

| 전이 | 로그 한 줄 | 근거 |
|---|---|---|
| 작업 전환 · 데이터 교체(자동 초기화) | **적는다** | 사용자가 요청하지 않은 소멸이라 흔적을 남긴다 |
| 선택 · 규칙 변경(강등) | 안 적는다 | 결과가 화면에 그대로 있다 — 같은 말을 두 번 한다 |
| 「결과 닫기」(명시 파기) | **안 적는다** | 사용자가 「치워라」라고 한 소멸이다. 치우라는데 흔적을 남기면 그 행동이 반만 듣는 것이 된다 |

이 한 줄도 **세션 스코프**다(로그 자체가 그렇다) — durable 원장이 아니다. 영구 증거는 생성 폴더에
쌓이는 `fill-ledger.json` sidecar 가 이미 진다.

**계약 영향**: `RESULT` 파기 조건이 갈리므로 `sessionKey` 를 **성분별로 비교**해야 한다 — 지금은
5성분을 `join("|")` 한 단일 문자열이라 **무엇이 갈렸는지 모른다**. 문자열 하나로는 이 판정을 못
지으므로 성분을 구조로 올린다(값·비교는 그대로, 모양만). `markResultStale` 은 살고
`resetGenResult` 의 호출자가 하나 는다.

### ~~2.19 데이터 표 열 선별 — 마일스톤 L 과 충돌하지 않는다 (사용자 확정 2026-07-30)~~ — **조치완료**(#358 · `73627e6`)

③에서 승계(§5.2 「데이터 표 열 선별」). 원장은 *"마일스톤 L 의 열 정책 「선별→고정」 결정과 충돌
여부를 먼저 확인해야 한다"* 로 열어 뒀다. **확인 결과 충돌은 한 자리뿐이고, 그 자리에 선을 그으면
사라진다.**

**⑴ 기각된 것은 다른 축이다.** #271(마일스톤 L 슬라이스 2) 원문 — *"열 정책 — 「선별」이 아니라
「고정」(토의 확정) · **자동 선별**은 선두 「문서」 열의 식별 요약이 담당 · **열 자동 선별을 시트에
이식하지 않는다**"*. 기각 대상은 **앱이 대표 열을 고르는 것**이고, U2 요청은 **사용자가 볼 열을
고르는 것**이다.

| #271 이 정한 것 | 이 판정과의 관계 |
|---|---|
| 자동 선별 기각 | **다른 축** — 사용자 선별은 판정된 적이 없다 |
| 식별 요약 = 좁은 면의 보상 압축 | **유지** — 선별은 그 위에 얹히는 사용자 도구 |
| 선두 식별 열 왼쪽 sticky = 스캔 앵커 | **유지** — 그 열은 **숨길 수 없다**(앵커가 사라지면 선별의 값도 사라진다) |
| 가로 스크롤 무경보 | **유지** — 「잘렸다」고 말하지 않는 것과 「사용자가 줄일 수 있다」는 양립한다 |
| **시트 = 전체 진실(전 열·원본 순서)** | **여기서만 부딪힌다** → 아래 |

**⑵ 선을 긋는 자리 — 선별은 인라인 표에만.** ⤢ 시트는 **전 열 그대로**다(사용자 확정). 두 면의
역할 분담이 그대로 산다: **인라인은 좁아서 압축이 필요한 면, 시트는 전체 진실을 보는 면.** 막히면
⤢ 로 전 열을 본다 — 선별이 무언가를 **가리는** 도구가 아니라 **인라인의 시야를 사용자가 정하는**
도구가 되는 이유다. 그래서 「이 열 숨기기」 항목은 인라인의 열 패널에만 서고, 시트로 이사한
패널에는 서지 않는다.

**⑶ 자리는 이미 있다.** `#jobColPanel`(열 머리 ▾)이 **열별 조건을 이미 소유**하고, ⤢ 가 열릴 때
그 패널째 시트로 이사한다(`job.js:859` `{id:"jobColPanel", slotId:"dataSheetSlot"}`). 되돌리기는
필터 칩 줄의 관용구를 재사용한다 — 「열 3개 숨김 ×」. **새 표면을 짓지 않는다.**

**⑷ 숨김은 표시 축뿐이다 — 이것이 이 항목의 confirm-or-alarm 자리다.** 숨긴 열도 **필터·검색·
매핑·생성에 그대로 참여**한다. 사용자가 「숨겼으니 문서에 안 들어간다」로 읽으면 **법적 효력이 있는
문서가 조용히 틀린다.** 그래서 ⑴ 숨김 표지는 **상시**(0개가 아니면 칩이 선다) ⑵ 문안이 축을
말한다(「보기에서 숨김 — 생성에는 그대로 쓰입니다」) ⑶ 「전체 열 검색」은 숨긴 열도 계속 매치한다
(숨긴 열의 값으로 행이 걸리는 것이 정상이고, 그 사실이 숨김≠제외의 증거다).

**⑸ 수명 = 세션 소유(필터와 같은 계층)**(사용자 확정). 데이터 교체 시 소멸한다 — 근거는 필터가
이미 같은 이유로 재생성된다는 것이다(`data_zone.py:_install_filter`: *"데이터 교체 = 필터 재생성
(**열 지형이 바뀐다**)"*). 열 선별은 **정확히 같은 의존**을 갖는다. 부수 소득으로 **신규 durable
필드 0개**이고 §5.3 의 「데이터별 선호는 뒷문」 경계를 건드리지 않는다.

**판정 주체는 링1**이다. `table_snap["columns"]`(`data_zone.py:461`)에 표시 여부를 얹고, 표면은
그리기만 한다 — 링2 가 「어느 열을 그릴까」를 자기 상태로 판정하면 시트·인라인·칩이 각자 답을
갖게 된다(§2.18 이 방금 정리한 「두 층이 같은 상태를 판정한다」의 재발).

**#295 와의 관계**: 인접 이슈지만 다른 물건이다 — #295 는 **그리드 기본값 3종**(원본 순서·최하단
시작·무선택)이고 열 축이 아니다. 다만 둘 다 「이 표는 사용자 파일이다」라는 같은 정신에서 나온다.

### ~~2.20 템플릿 어포던스 — 이관은 아이콘 2개, 신설은 한 곳 (사용자 확정 2026-07-30)~~ — **조치완료**(#359 · `1c1fe7d`, §4 판정 A~D·F 와 같은 PR)

③에서 승계(§5.2 「템플릿 어포던스의 detail 이관」). §4 판정 A 가 메인 존을 처분하며 승계처를
지정했고, 여기서 **무엇이 옮겨지고 무엇이 새로 지어지는지**를 확정한다.

**⑴ 실물 — 라벨 없는 아이콘 2개다.** `PathTrack.affordances(path)`(`pathtrack.js:23`)의 기본
묶음은 `open`=「열기」 · `reveal`=「폴더에서 보기」이고(`copy`=「경로 복사」는 기본 밖 — 저장
폴더·결과 쪽 전용), 라벨은 `title`·`aria-label` 에만 산다. 피드백 문장의 「템플릿 폴더 열기/파일
열기」와 **어휘가 다른 것**은 오해가 아니라 **아이콘이라 라벨이 화면에 없기 때문**이다.

**⑵ 죽는 자리는 하나** — 메인 side-card 「선택한 작업」 존의 템플릿 줄(`job.js:188`
`템플릿 {이름} + PathTrack.affordances(s.template_path)`). §4 판정 A 가 존 전체를 죽이므로 함께 간다.

**⑶ 승계처 둘 — 하나는 다른 판정 소관, 하나가 이 항목의 몫이다.**

| 승계처 | 소관 | 성격 |
|---|---|---|
| **활성 카드 ⋮** 의 「열기」·「폴더에서 보기」 | §4 판정 B | 그 판정이 이미 자리를 정했다(전 카드에 주면 hover 노출이라 발견성이 더 나쁘다) |
| **라이브러리 상세** | **이 항목** | **신설** — `library.js` 전체에 `PathTrack` **0건**이고 상세는 `<dt>템플릿</dt><dd>{이름}</dd>` 로 **이름 텍스트만** 그린다 |

라이브러리 상세가 「계기판의 짝」인 이유가 여기 있다 — **경보는 라이브러리가 내는데**(「템플릿이
연결되지 않은 작업 N건이 있습니다」) 그 파일을 열거나 폴더로 갈 길이 그 화면에 **없다.**

**⑷ 이관되지 않는 것**: 편집기 「템플릿」 탭의 같은 어포던스(`editor.js:592`)는 **그대로 산다.**
§4-A 의 실측이 메인 존을 *"이관 대상이 아니라 **세 번째 사본**"* 이라 판정했고, 남는 두 거처가
편집기 탭과 (신설되는) 라이브러리 상세다. **사본을 셋에서 둘로 줄이는 것이지 옮기는 것이 아니다.**

**⑸ 어휘는 바꾸지 않는다**(이번 판단). 피드백의 「템플릿 폴더 열기」는 **대상을 말해 달라**는
요구인데, 라이브러리 상세에서 그 아이콘은 `<dt>템플릿</dt>` **행 안에** 선다 — **자리가 대상을
이미 말한다.** 그리고 `PathTrack` 은 화면-불가지 공용 헬퍼라 한 소비처의 사정으로 라벨을 바꾸면
**네 소비처가 함께 바뀐다**(§2.1 「공용 층이 곁의 규칙을 덮는다」 계열). 실앱 한 바퀴에서 여전히
안 읽히면 그때 **PathTrack 전역의 어휘 결정**으로 승격하고, 이 자리에서 국소로 바꾸지 않는다.

**계약 영향 — 웹 한 줄이 아니라 payload 한 칸이 선행이다.** `screen_library.py:227` 이 상세에
**`template_name` 만** 싣는다(전수 대조 — `template_path` 0곳). 즉 `library.js` 에
`PathTrack.affordances(d.template_path)` 를 부르려면 **링2 payload 신설**이 먼저다. 경로 검증은
백엔드 화이트리스트(`app.py _validate_owned`)가 이미 소유하고 클릭 위임도 문서 레벨이라, 그 한
칸 외에 새로 짓는 배선은 없다.

---

## 3. ② 재등급 — 「실재하지 않음」이 아니라 「결함 소재가 다르다」

②를 다시 세면 **조치 0인 것은 1건뿐**이다. 나머지는 설계 판정이 살아남되 사용자가 그렇게
읽었다는 사실이 **표면의 실패 증거**이므로, 소재만 옮겨 ①·③으로 재배치된다.

| 항목 | 살아남는 판정 | 그럼에도 남는 결함 | 재배치 |
|---|---|---|---|
| TXT에 미리보기 불필요 | §10.15 판정 J가 배제 선언했고 거울 존도 `display:none`(`job.js:465`) | 없음 | **조치 0** |
| ~~「선택한 작업」 불필요~~ | 템플릿 파일명·열기 어포던스·재연결 경보가 실질 | 중복인 작업명이 굵고(`job-head-title`) 실질인 템플릿이 `muted` — **위계 역전** | §4에서 **확정** — **조치완료**(#359 · `1c1fe7d`, 판정 A~D·F) |
| ~~되돌리기 `↩` 불필요~~ | 유일한 행 단위 자동 제안 복귀 경로. 대체재로 지목된 「일괄 초기화」는 **존재하지 않는다**(「모두 해제」는 확정만 풀고 수동 지정은 남는다) | 아이콘이 `↩`(undo)라 **되돌리기로만 읽힌다** — 같은 사용자가 「수동 재추천 버튼이 없다」고 따로 요청했다. **기능이 이름에 가려졌다** | **① 승계**(§2.6 확정) — **조치완료**(#334 · `3469d44`) |
| ~~파일 찾아보기 ≡ 직접등록~~ | 직접등록은 파일 대화상자가 아니라 이름·경로·시트·메모 **수동 입력 폼** | 그 폼 **안의** 「찾아보기…」가 같은 네이티브 피커를 열고 라벨이 「파일 찾아보기…」/「찾아보기…」로 거의 같다 — **표면 어휘상으로는 관찰이 정확하다** | **① 승계**(§2.7 확정) — **조치완료**(#361 · `0e5cac1`) |
| ~~등록 후 바로 메인으로 나감~~ | 직접등록은 모달을 유지한다(「이 데이터 사용」 2클릭 = 제안대로) | 본 것은 「파일 찾아보기」 경로이고 **그 경로엔 고정 기회가 없이 지나간다** | **① 승계**(§2.7 확정 — 위 항목과 **한 조치**) — **조치완료**(#361 · `0e5cac1`) |
| 「아직 한 번도…」 불필요 | 검토를 요구하는 **이유 문장** | 자리가 틀렸다 (§2.3) | **미조치 확정**(§2.10 말미에 짓지 않은 안 셋) |
| ~~두 트리거 차이 소명~~ | 필드축 ack(`run_state.py:512`) vs 규칙축 검토(`review_state.py:148`) — 다른 축이 맞다 | **소명을 요구했다는 사실이 결함이다.** 거울 "눌러서 **확인**해야 생성할 수 있습니다" / 드로어 "결과 **확인** 완료" — 같은 동사가 두 축에 겹친다 | **① 승계**(§2.10 확정) — 어휘·지목만 **조치완료**(#335 · `5e0b7bb`). **형상은 §2.13 이 닫았다**(2026-07-30 — 필드축 ack 폐기로 **두 축이 하나가 된다**) — 그 형상도 **조치완료**(#364 · `59d1d8a`) |
| ~~종료 가드가 항상 뜬다~~ | 판정은 조건부(`data_zone.py:351 _selection_guard`) | **정상 워크플로가 정확히 그 조건**(몇 행 수동 선택 → 생성 안 하고 종료)이라 실질은 맞다 | **① 승계**(§2.9 확정) — **조치완료**(#356 · `4b5f7e7`) |

**후속 규칙 — 소명이 서면 그 소명이 결함의 증거다.** 계약 문서로 변호할 수 있다는 사실은,
앱을 쓰는 사람이 그것을 알 길이 없다는 뜻이다. ②를 「기각 목록」으로 닫으면 진짜 결함을 놓친다.

### 참고 — 확인 게이트의 서열 (반복 질문 대비 · §2.13 착지 후 형상)

```
드리프트(danger) > 파일명 토큰(danger)
  > 전제조건(warn) > 검토 요구(warn — 규칙 변경 · 빈 값 blank_set) > 열림
```

- 구 「본문 확인」 ack 단은 **§2.13 착지(#364 · `59d1d8a`, 이슈 #346)로 사라졌다** — 필드축 ack 가 폐기되면서
  두 축이 하나가 됐다. 빈 값은 이제 검토 요구의 한 위험종(`blank_set`)이다: 규칙이
  기준선과 같아도 이번 실행 입력에 빈 값이 있으면 요구가 서고, 승인 지문에 **빈 값 필드
  집합의 해시**가 들어 집합이 갈리면 승인이 자동 무효다(침묵 금지).
- **확인 면(생성 값 미리보기 시트)** = 유일한 확인 표면. 규칙축(저장된 규칙 지문 vs 마지막
  완주 기준선, `review_state.py`)과 빈 값축(blank_set)을 한 승인이 진다 — 표식 삽입 동의는
  승인 1번이 겸한다(ack 의 정체가 「표식을 박고 진행하겠다」였으므로 성질은 같고 횟수만
  줄었다).

한 번 완주하면 규칙축 요구는 영구히 조용해진다(§10.12 판정 N: "업그레이드 비용은 작업당
1회"). 빈 값축은 데이터의 사실이라 그 뒤로도 빈 값이 있는 실행마다 선다 — "정상 루프에서
보일 일 없다"는 관찰은 규칙축엔 참, 빈 값축엔 거짓이다.

---

## 4. 판정 — 「선택한 작업」 존 사망과 후보 카드 승계 (사용자 확정 2026-07-29) — **A~F 전부 조치완료**(#359 · `1c1fe7d` · #366 · `824b40a`)

**전부 착지했다**(2026-07-31). 판정 A~D·F 가 먼저 착지했고 **판정 E(needs 리다이렉트)가 뒤따랐다** —
전제인 `DEFERRED_ENTRY_REASONS` 한 줄 삭제가 §2.4 「이 데이터로 신규 작업」과 **같은 해제**라
둘이 한 이슈(**#349** · PR **#366**)로 묶였다. 잔여를 세는 자리는 §1.1 표 하나다.

### ~~A. 「선택한 작업」 존(`index.html:336-341`)은 사망한다~~ — **조치완료**(#359 · `1c1fe7d`)

근거는 **「메인을 어설픈 계기판으로 만드느니 계기판을 계기판으로」**(사용자 확정). 실측이
이를 강화한다 — 메인의 어포던스는 이관 대상이 아니라 **세 번째 사본**이었다:

| 기능 | 메인 존 | 라이브러리 상세 | 편집기 「템플릿」 탭 |
|---|---|---|---|
| 템플릿 이름 | O | O (`library.js:290`) | O |
| 열기·폴더에서 보기 | O | **없음** | O (`editor.js:548`) |
| 「템플릿 다시 연결…」 | O | O (`library.js:271`) | — (공용 `Relink`) |

라이브러리 경보 문안이 이미 같은 문법을 쓴다 — *"템플릿이 연결되지 않은 작업 N건이 있습니다.
「확인 필요」 보기에서 조치하세요."* **경보는 여기, 조작은 저기.**

**사망 조건 점검표** — 전 행이 새 거처에서 도달 가능함을 확인한 뒤에만 지운다:

| 죽는 것 | 승계처 |
|---|---|
| 작업명 표시(`#jobHeadTitle`) | 후보 카드 `.active` 하이라이트 — **이미 존재** |
| 템플릿 파일명(`#jobHeadTpl`) | **활성 카드**의 확장 부제 (판정 B) |
| 열기·폴더에서 보기(`PathTrack`) | **활성 카드 ⋮** (판정 B) + **라이브러리 상세에 신설** — 계기판의 짝. 현재 `library.js`에 `PathTrack` 0건 |
| 「템플릿 다시 연결…」 버튼 | 경고 카드의 **기본 클릭 대체** (판정 D) |
| `template_missing` 경보 | 카드 **「연결 상태」** 텍스트 + 경고색 (판정 C) |
| 「템플릿 경로가 비어 있습니다…」 | 같은 「연결 상태」 축 |
| 「문서 작업을 선택하면…」 빈 상태 | 후보 구획 빈 상태가 이미 소유 |

**상시성의 상속**(§5.2 sticky 기각이 여기로 넘긴 것). 존이 죽으면 「지금 **어느 작업으로** 생성
하는가」를 말하는 표면은 **활성 카드 하이라이트 하나**인데, 그 카드는 우열 맨 위라 **표를 훑으면
스크롤 위로 사라진다.** 지금도 존이 같이 사라지므로 새 결함은 아니지만, **sticky 사이드바를
기각했으므로 그 자리를 메우는 것은 사이드바가 아니다.** 후보는 상수 높이 층인 **액션바가 작업
이름을 겸하는 것**이다 — 지금 「이 작업으로 문서 생성」은 **그 작업이 무엇인지 말하지 않는다.**
짓는지는 §4-A 착수에서 판단하고 여기서 미리 짓지 않는다.

### ~~B. 카드 ⋮ 와 확장 부제는 활성 카드에만 선다~~ — **조치완료**(#359 · `1c1fe7d`)

§10.1 S1의 카드 항목(`이름·방식·연결 상태·마지막 성공 실행·즐겨찾기·⋮`) 중 **⋮ 와 연결
상태는 계약이 자리를 잡아뒀는데 안 지어진 칸**이다 — `candCard`는 이름·방식·마지막 실행·별만
구현한다. 전 카드에 주면 side-card에 ⋮ 다섯이 서고, 이 계열 버튼은 `visibility:hidden` +
hover 노출이라 발견성이 나쁘다(F8 주석이 *"노출 규칙이 없으면 기본 hidden 이라 영영 안
보인다"*고 자백). 활성 카드만 확장하면 죽는 존의 의미론(**선택된 하나의 정체**)을 카드가
그대로 승계한다.

### ~~C. 「연결 상태」는 텍스트가 정본이고 색은 강조다~~ — **조치완료**(#359 · `1c1fe7d`)

`candCard` 주석이 이미 계약을 적었다 — *"색만으로 방식을 구별하지 않는다(텍스트가 늘 함께
선다)."* 따라서 경고색 하이라이트는 **「연결 상태」 신설과 한 묶음**이다. 존을 죽이는 것과
연결 상태를 짓는 것은 분리할 수 없다.

### ~~D. 재연결은 메뉴 항목이 아니라 기본 클릭의 대체다~~ — **조치완료**(#359 · `1c1fe7d`)

재연결이 필요한 카드는 경고색으로 서고, 클릭하면 선택 대신 **안내와 함께 재연결로
리다이렉트**된다. 부수 규칙 셋:

- 활성 + 경고가 겹치면 **경고가 이긴다** — 선택 사실은 다른 단서로도 서지만 차단 사유는
  여기서만 말한다
- 클릭 의도(선택)와 실제 동작(재연결)이 다르므로 **왜 다른지 재진술**한다(다이얼로그가 겸한다)
- 재연결 성공 뒤 **이어서 선택**한다 — confirm-or-alarm 완화 조항(전면 가시성·틀리면 즉시
  보임). 순서는 브리지 발신 규약: 재연결 커밋이 성공한 **뒤에** 선택이 한 체인으로 나간다.
  실패하면 선택하지 않고 카드는 경고로 남는다

### ~~E. needs 리다이렉트는 2분기다 — §18.7 6분기 중 나머지 넷은 짓지 않는다~~ — **조치완료**(#366 · `824b40a`)

계약 표(`docs/core-workflow.md` §18.7)를 펴고 master 실측과 대조한 결과:

| # | 계약 원인 | 사용자 행동 | master 실측 | 처분 |
|---|---|---|---|---|
| 1 | Binding drift | 연결 복구 | needs 사유로 없음 — 선택 후 게이트가 danger 차단 + `openEditForRepair` | **이미 다른 층에서 풀림** |
| 2 | 복원 가능한 `default_dataset_ref` | 기본 데이터 사용 | #53-A 자동 조준(`data_notice`, §8.2 ③ 현행 유지) | 데이터 축이 소유 |
| 3 | 끊어진 **데이터** 참조 | **데이터** 참조 다시 연결 | 데이터 선택 다이얼로그 고정 행 「다시 연결…」(F1) | 데이터 축이 소유 |
| 4 | **다른 데이터 구조**의 저장 작업 | 이 템플릿으로 새 작업 | **master `needs_action`의 유일 원인** — `missing = 현재 데이터에 없는 필수 소스 키`(`work_candidates.py:73-75`) | **짓는다** |
| 5 | Template-only 자산 | 새 문서 작업 만들기 | 후보엔 작업만 뜬다 | 후보 목록의 정체를 바꾸는 **별개 결정** |
| 6 | Job/Template 손상 | 지원 범위 확인 | `excluded(unsupported)` + 손상 격리 | 계약이 **리다이렉트를 금지**: *"연결 문제로 추측하지 않고 실제 점검 필요 상태 표시"* |

4행의 목적지는 **`document_browser_new_work`** 인데 이 사유는 `DEFERRED_ENTRY_REASONS`에
남아 fail-closed 차단 중이다. F8은 종결됐으므로 **그 줄을 지우는 것이 배선의 전제**이고,
그 한 번의 해제가 needs 카드 리다이렉트와 「이 데이터로 신규 작업」(§2.4)을 **동시에** 연다.

따라서 needs 구획은 사유별 2분기를 갖는다:

| 사유 | 「연결 상태」 문안 축 | 클릭 목적지 |
|---|---|---|
| 데이터 구조 불일치 | 없는 열 열거 | 새 작업 마법사 |
| 템플릿 부재 | 템플릿 없음 | 재연결 |

### ~~F. 템플릿 부재를 후보 축에 올리는 것은 계약과 다른 배치다 — 근거는 전제 차이~~ — **조치완료**(#359 · `1c1fe7d`)

§18.7 6분기에 **템플릿 재연결은 없다**(3행의 「다시 연결」은 **데이터 참조**다). §18.4가
선을 긋는다:

> `available`은 최소 Binding 호환성일 뿐 (…) **Template 읽기**, 구조 drift, 파일 이름 토큰
> (…)은 작업 선택 뒤 권위 있는 `validateRun`에서 따로 검사한다.

즉 판정 D는 계약의 미구현분을 채우는 것이 아니라 **다른 배치를 택하는 결정**이며, §10.0의
「계약이 이긴다」에 대한 **명시적 예외**다. 근거는 전제가 달라졌다는 것이다: 계약이 후보 축
밖에 둔 이유는 v6 세계에서 Template 읽기가 비싼 검사였기 때문인데, master는 이미 그 판정을
싸게 갖고 있다(`library_health`의 `missing_template_count`). **눌러본 뒤에 차단하는 것은
뒤늦은 경보다.**

「계약 항목을 짓기 전에 그 문제가 여기서 이미 어떻게 풀리는가를 센다」 규칙의 **역방향 표본** —
세어 보니 계약보다 **더 이르게** 풀 수 있었다. lab `core-workflow.md` §18.4 쪽에 정정 표기를
넣을지는 열어 둔다.

---

## 5. ③ 미결 — 토의·검증이 남은 것 — **③ 소진(0건) · 승계분은 ①에서 전부 착지**

**이 장의 현재 뜻은 「미결」이 아니라 「미결이었던 것의 계보」다**(2026-07-30). ③ 14건은 13건이
①(§2.11~§2.20)로 승계되고 1건(splitter·sticky)이 기각돼 **판정 잔여 0**, 그 승계분은 **전부
코드에 착지**했다 — 각 행이 목적지 절과 커밋을 든다. **마지막으로 남아 있던 §5.3 판정 B 도
착지**(#348 → #368 · `18f84c4`)해 이 장에 미착지는 없다. 잔여를 세는 자리는 §1.1 표 하나다.

### ~~5.1 재현이 선행돼야 하는 것~~ — **판정 이동 → §2.11**(원인 확정 2026-07-30)

이 절은 *"`web/css/` 전체에 표 행 `transform`/`translate`/padding 변경이 없다"* 고 적었다.
**틀렸다** — `base.css:106` 에 정확히 그것이 있었다. 조사가 존의 소유자 파일(`jobdata.css`)과
스크롤 보존 경로만 훑고 **공용 모션 층**을 안 봤다. §2.1 과 같은 계열의 실패다: 결함이 그
표면의 파일이 아니라 **그 표면과 이웃이 나눠 쓰는 층**에 있었다. 원래 적힌 후보(열 폭
재계산·세로 스크롤바 출현)는 기각이다 — 둘 다 **누르는 동안만** 이라는 조건을 못 만든다.

**증상 재진술이 원인을 지목했다.** 결정적 관찰은 사용자가 뒤에 준 *"클릭을 **유지**하면 레코드
전체가 **중앙으로** 모인다"* 였다. 지속 조건이 `:active` 를, 좌우 대칭 수축이
`transform-origin:center` 를 지목한다 — 「우측으로 움직인다」 하나만으로는 열 폭 재계산과
구별되지 않았다. **재현이 아니라 증상의 한 문장이 후보를 갈랐다.**

### 5.2 설계 택일이 남은 것 — **전 행 판정 이동 완료 · 이관분 전부 착지**(splitter·sticky 는 기각 · 부트스트래핑 broad 는 별건 #351)

| 항목 | 갈리는 지점 |
|---|---|
| ~~splitter + sticky 사이드바~~ | **둘 다 기각**(2026-07-30 확정 · 재평가 완료). 이 행이 적어 둔 sticky 근거 *"우 side-card 가 게이트·검증·실행을 소유"* 는 **낡은 서술**이다 — 미리보기·승인 표지·생성·**게이트 사유(`#jobGate`)** 는 전부 **하단 액션바**가 갖는다(`job.css:86` `position:sticky;bottom:0`, #179 5b). ⑴ **sticky 기각**: 우열에 남는 것(후보 카드·저장 폴더)은 스크롤과 왕복하는 물건이 아니라 **한 번 정하고 끝나는 설정**이고, §4-A(존 사망)·§2.13(`#jobRestate` 이주) 뒤엔 더 줄어든다. 결정적으로 **붙일 것이 뷰포트에 드는지가 후보 수(1~5장)·창 크기에 따라 갈려** 조건부로만 붙는다 — 액션바가 옳은 자리였던 이유는 그 층의 **높이가 상수**라는 것이다. 조건부로 붙는 고정은 사용자에게 규칙을 못 준다. ⑵ **splitter 기각**: 기제는 **살아 있다**(CSS `job.css:173-178` · 드래그 `app.js:157` · 브리지 `setMasterWidth` · durable `master_width` · 복원 `personalization.js:19` · 프로브). DOM 소비만 0이고 그 사실은 `test_web_selftest_gate.py:1118` 의 `splitters == 0` 에 **선언**돼 있다 — 그러므로 기각 근거는 **비용이 아니다**. 폭 조절이 겨누는 「데이터 표가 좁다」의 원인은 **열 수**이고 그 처분은 아래 **「데이터 표 열 선별」(#295)** 이 소유한다. 폭은 완화, 열 정책은 해소 — 둘 다 지으면 같은 증상에 손잡이가 둘이 되어 어느 쪽이 일한 것인지 못 센다(§2.1 「원인이 하나면 조치도 하나」). **원문이 남긴 것은 제안이지 이유가 아니므로** 판정은 「그 문제가 여기서 이미 어떻게 풀리는가」를 세는 리트머스로 했다. `master_width` 배선은 **지금처럼 선언된 미완으로 존치**한다(다음 master-detail 표면의 몫) |
| ~~복사 완료 건 후미 이동~~ | **판정 이동 → §2.15** — 그 판정은 **조치완료**(#357 · `4592f35`) — 2택 중 **「고정 순서 + 상태 색」**으로 확정(2026-07-30). 택일이 아니라 **한 화면에 이미 갈려 있는 두 순서 중 하나를 지우는 것**이었다: 사람이 읽는 자리는 고정 사본, 점 띠·이동 경계는 큐 순서 |
| ~~「직접 등록」 처분~~ · ~~「파일 찾아보기」 경로의 고정 기회~~ | **판정 이동 → §2.7** — 그 판정은 **조치완료**(#361 · `0e5cac1`) — 둘이 한 조치로 수렴했다(2026-07-29 확정). 삭제 근거는 중복이 아니라 `pickPoolDataFile`이 **경로만 반환하고 로드하지 않는다**는 것이었고, 그 고유 기능이 곧 결함이라 대체 경로 신설 없이 삭제된다 |
| ~~부트스트래핑~~ | **판정 이동 → §2.16** — narrow 는 **조치완료**(#355 · `29800cc`) · broad 는 별건 **#351** 로 열려 있다 — 범위가 **narrow(폴더 일괄 등록) 확정 · broad(template+mapping 패키지) 별건 승격**으로 닫혔다(2026-07-30). broad 의 선행 §5.3 판정 B 는 그 뒤 **착지**(#368 · `18f84c4`)해 선행 조건이 풀렸으나 **이식 규칙 자체가 새 결정**이라 여전히 조건부 판정이고, **#351 은 별건으로 열린 채**다. 이슈 **#284** 는 인접하되 다른 물건(배포본 동봉 예제 vs 사용자 자산) |
| ~~저장 후 2단계 착지~~ | **판정 이동 → §2.14** — 그 판정은 **조치완료**(#354 · `da474fe`). *"버그가 아니라 의도된 코드"* 는 참이나 **의도의 출처가 판정이 아니라 승계**였고, 여기 적힌 되깎기 조건(과거 결함 재발 경로 차단)은 **이미 충족돼 있다** — 그 결함은 `_reset()` 의 것이고 지금 착지는 재로드 + notice 채널이 각각 막는다. **제자리 착지로 통일**(2026-07-30 확정) |
| ~~취소·저장 문법~~ | **판정 이동 → §2.17** — 그 판정은 **조치완료**(#354 · `da474fe`) — 2택 중 **오버레이(단일 출구) 유지**로 확정하되, 실측이 찾아낸 **진짜 어긋남 하나**(같은 `dirty` 축을 저장은 「상시+비활성」, 버리기는 「나타났다 사라짐」으로 말한다)를 국소 수선한다(2026-07-30) |
| ~~확인 2면 재편~~ | **판정 이동 → §2.13** — 그 판정은 **조치완료**(#364 · `59d1d8a`). 어휘·지목은 §2.10 이 먼저 가져갔고(#335 · `5e0b7bb`) 남은 형상이 2026-07-30 에 닫혔다 — **§5.4 의 2 pane 안은 기각**되고 **1 pane + 필드축 ack 폐기**로 확정 |
| ~~데이터·문서 재선택 시 결과 처분~~ | **판정 이동 → §2.18** — 그 판정은 **조치완료**(#363 · `141fbf1`) — **지문 성분별로 처분을 가른다**(2026-07-30 확정): 작업 전환·데이터 교체는 **초기화**(링1 이 이미 증거를 죽이고 CLAUDE.md 세션 계약도 그렇게 말한다), 선택·규칙·저장 폴더는 **강등 유지**(판정 G 의 근거가 사는 축). 저장 위치 도달성은 실행 기록 한 줄이 진다 |
| ~~종료 가드 처분~~ | **판정 이동 → §2.9** — 그 판정은 **조치완료**(#356 · `4b5f7e7`) — 원문 3안 중 **②조용한 손실**로 확정(2026-07-29). ①세션 복원 기각 근거는 비용이 아니라 **정직성**(인덱스 복원이 바뀐 파일의 다른 행을 조용히 가리킨다), ③조건 축소 기각 근거는 술어가 이미 정밀하다는 것 |
| ~~휴지통~~ | **판정 이동 → §2.12** — 그 판정은 **조치완료**(#353 · `6f4c646`) — 3택 중 **문안 정직화**로 확정(2026-07-30). 어휘를 6자리에서 내리고 30일은 **표면에 쓰지 않는다**. 슬롯 영속화는 기각(휴지통 표면의 열등 변종), 휴지통 표면은 **별건 승격** — 근거 둘(관용 부등식 · 그룹 소실)은 §2.12 가 보관 |
| ~~데이터 표 열 선별~~ | **판정 이동 → §2.19** — 그 판정은 **조치완료**(#358 · `73627e6`) — 마일스톤 L 과의 **충돌 없음**이 확인돼 **짓는다**(2026-07-30). #271 이 기각한 것은 **앱의 자동 선별**이고 요청은 **사용자 선별**이라 축이 다르다. 부딪히는 자리 하나(「시트 = 전체 진실」)는 **선별을 인라인 표에만** 두어 해소 |
| ~~템플릿 어포던스의 detail 이관~~ | **판정 이동 → §2.20** — 그 판정은 **조치완료**(#359 · `1c1fe7d`) — 이관 대상은 아이콘 2개(「열기」·「폴더에서 보기」)이고, 실제 **신설은 라이브러리 상세 한 곳**뿐임이 확정됐다(2026-07-30). 어휘는 **바꾸지 않는다**(자리가 대상을 말한다) |

### 5.3 저장소 정체성 — 데이터 축은 **경로가 정체성**, 작업↔데이터 결속은 폐기 — **B~D 전부 조치완료**(#362 · `27f7b0a` · #368 · `18f84c4`)

§2.8 을 파다가 *"풀이 이름을 키로 드는 게 이상하다"* 는 질문이 나왔다. 첫 판정(2026-07-29)은
「이름=키 유지 · id 기각」이었고 **2026-07-30 토의에서 데이터 축에 한해 뒤집혔다.** 그 결과
§2.8 이 폐기됐으므로 이 절은 미결 설계가 아니라 **라운드가 낳은 판정**이다.

**관찰 — 한 저장소에 식별 관례가 셋이다.**

| 대상 | 식별 | 링크를 거는 쪽 | 판정 후 |
|---|---|---|---|
| 작업 | 이름(slug 파일명) | — | **불변** |
| 데이터 참조 | 이름(slug 파일명) | 작업 → 데이터 = **이름**(`default_dataset_ref`) | **경로+시트가 정체성 · 링크 폐기** |
| 템플릿 | **절대경로** | 작업 → 템플릿 = `template_path` | 판정 B(상대키) 그대로 |
| 템플릿(그룹 지정) | **홈 상대경로** | 그룹 → 템플릿 = `rel_key(path, root)`(`template_groups.py:44`) | **불변** |

**판정 A(개정) — 이름=키는 유지하되 적용 범위가 작업·템플릿 축이다. 데이터 축에서는 철회한다.**

첫 판정의 근거는 이식이었다 — 저장 관례(`slug` 파일명 + `ensure_ascii=False` + `indent=2`)와
위치-불가지가 겨눈 것이 **「폴더가 곧 이식 단위」** 이고, 코드도 그렇게 적어 뒀다
(`screen_editor.py:1815-1816`): *"등록 실패해도 참조 이름은 안정적이라 사용자가 그 이름으로
수동 등록하면 링크가 완성된다"*.

그 논거는 참이지만 **데이터 축에 잘못 배치됐다.** 이식해야 하는 것은 템플릿+매핑이고(판정 B 가
이미 그렇게 말한다) **남의 PC 에 내 엑셀 파일은 없다.** 데이터는 이식 대상이 아니라 각자
**자기 것을 연결하는 것**이다. 같은 절이 템플릿 축에서는 자동 파일명 매칭을 *"다른 폴더 동명
파일 오연결로 악명"* 이라며 기각해 놓고 데이터 축에서는 그 기제를 이식 근거로 든 모순도 여기서
풀린다 — **두 축 모두 이름 자동 결속을 안 쓴다**로 일관된다.

**~~판정 C — 데이터 축 정체성은 `normcase(abspath(path)) + sheet` 다.~~** — **조치완료**(#362 · `27f7b0a`, 파괴적)

- `name` 은 **순수 라벨**로 강등한다 — 중복 허용·개명 자유·정체성 무관.
- `_slug` 파일명과 `guard_slug_collision` 의 **데이터셋 소비자가 소멸**한다(작업 축은 그대로).
- 시트가 축에 드는 것은 새 발명이 아니다(#33) — 같은 워크북의 다른 시트는 다른 데이터다.

**~~판정 D — 작업↔데이터 결속(`default_dataset_ref`, #53-A)을 폐기한다.~~** — **조치완료**(#362 · `27f7b0a`, 파괴적)

이 필드는 **v6 데이터-우선 재작성을 살아남은 작업-앵커 잔재**다. 앱의 정본 방향은 이미
「데이터를 고르면 가능한 작업이 선다」이고(`gui/work_candidates.py`), 반대 방향으로 흐르는
배선은 이것 하나뿐이었다. 이름 정체성 문제가 여기서만 터진 이유이기도 하다.

| | |
|---|---|
| 죽는 것 | `Job.default_dataset_ref` · `_auto_aim_default`(`screen_job.py:1856`) · `_do_prefer_work` 의 `default_data` 분기(`:1761`) · 저장 시 데이터 **자동등록**(#18·#26)과 그 게이트(`screen_editor.py:_dataset_gate`) |
| 사는 것 | `source_keys()` 계약 · `compatibility_for`/`rank_available` 후보 판정 · 세션 소유 마운트 데이터 · 「이 데이터 고정」 **명시 등록** |
| 바뀌는 것 | 작업을 먼저 고르는 진입점(라이브러리 「문서 만들기에서 사용」)은 **데이터 선택을 반드시 지난다** — `_do_prefer_work` 는 「보관 후 안내」 하나로 단순해진다. F2 PR-B 판정 I 가 걱정한 「#53-A 도달 불가능」은 **#53-A 자체가 죽으므로 소멸**한다 |

**id 축(`dataset_id`) 기각의 근거는 「이름이 좋아서」가 아니라 「지킬 것이 없어서」다.**
`dataset_pool_state.update_excel_reference` 계약이 *"기존 항목의 참조(kind+opts)만 갱신한다
(**수명 보존**)"* 라고 못박는다 — **내용물 교체가 정상 수명 사건**인 개체다(월별 파일이면 매
사이클 갈아끼운다). 불변 id 는 삭제·재생성과 relink 를 구분해 주지만 **끝 상태가 같다.** 게다가
결속이 사라지면 정체성을 오해할 **소비자 자체가 없다** — 저장소 전수 대조상 풀 이름을
외래참조로 드는 곳은 `default_dataset_ref` 하나였다(원장은 들지 않는다).

**함께 기각 — 풀 항목의 스키마 지문 캐시 필드.** 「작업 → 후보 데이터」 방향이면 풀 항목 N개의
헤더가 필요해 캐시가 정당했다. 방향이 **데이터 → 작업**이면 읽는 파일은 사용자가 방금 고른
1개뿐이고 어차피 마운트하며 읽는다. **이 재편의 신규 durable 필드는 0개다.**

**이미 서 있는 것 — 이 판정은 신설이 아니라 잔재 제거다.**

| 모델 | 실측 |
|---|---|
| 작업은 데이터가 아니라 **스키마**를 기억한다 | 이미 참 — `Job` 에 데이터는 없고 `source_keys()` 가 계약. **유일한 이물질이 `default_dataset_ref`** |
| 데이터를 고르는 순간 스키마를 읽어 **가능한 작업을 제시**한다 | 이미 구현 — `compatibility_for`(`missing = source_keys − 현재 열`) + `rank_available` + 「확인 필요」가 빠진 열을 재진술 |
| 즐겨찾기는 **작업 소유** 속성이다 | 이미 그렇다 — `Job.favorited_at` 이 후보 정렬의 **1차 계층**(`TIER_FAVORITE`). 데이터별 즐겨찾기는 존재한 적 없다 |
| 데이터 목록은 **경로로** 관리된다 | **여기만 실제 변경** — 지금은 이름 slug 가 키고 경로는 내용물이다 |

**뒷문 금지**: 「이 데이터로 자주 쓰는 작업」류의 **데이터별 선호**를 나중에 만들면 그것이 곧
`default_dataset_ref` 의 재발명이다. 데이터↔작업 결속은 어느 방향으로도 다시 들이지 않는다.

**사용자가 내주는 것**(명시 확인, 2026-07-30): **「작업을 열면 데이터가 이미 서 있다」가
사라진다.** 매달 같은 파일을 쓰는 사람도 데이터를 고르고 후보에서 자기 작업을 고른다.
완화는 둘 — ⑴ 마운트 데이터는 **세션 소유**라 작업 전환에서 생존하므로 요구는 **세션당 1회**다
(지금도 자동 조준은 `datasource is None` 일 때만 돈다) ⑵ 후보가 1건이어도 **자동 결속하지
않고** 미리 골라 둔 채로 제시한다 — 여기서 자동으로 붙이면 원칙이 다시 샌다.

**마이그레이션은 무손실이 아니다.** 기존 `.dataset.json` 은 `opts.path` 를 들고 있어 새 키로
재배치되지만, **다른 이름·같은 경로 2건**은 병합 판정이 필요하다(조용히 하나 버리지 말고 loud
재진술 후 사용자 확정). 작업의 `default_dataset_ref` 는 마이그레이션이 아니라 **폐기**다 —
「이 작업이 지난번 쓰던 데이터」 정보가 사라진다.

**판정 B — 결함은 템플릿 축이다.**(**조치완료** — #348 → #368 · `18f84c4` · 라이브러리 루트
상대키 가산) 레지스트리는 위치-불가지인데 **내용물이 위치에 묶여 있다.**
홈을 옮기면 작업의 `template_path` 가 전부 깨진다. 증거가 경보 문안에 있다 — 라이브러리는
*"템플릿이 연결되지 않은 **작업 N건**"* 이라고 **작업 수**로 센다. 간접층이 있었다면 데이터처럼
「참조 1건」이었을 것이다. 데이터 축은 간접층 덕에 이식성을 갖고, 템플릿 축은 못 갖는다.

**처방 — 템플릿을 데이터 쪽에 맞춘다**(반대가 아니라):

- `Job` 에 **라이브러리 루트 상대 POSIX 키**를 가산 필드로(`조달/공고서.hwpx`). 매체는 접미사
  파생이 이미 계약(`core/job.py:204`)이라 **루트 선택도 확장자로** 하면 새 필드가 필요 없다
- 관례를 새로 만들지 않는다 — `rel_key` 가 이미 그 값을 만들고 그룹 지정이 쓴다. **표면도 이미
  키로 말한다**(`screen_editor.py:574`). 저장만 경로다
- **폴백 정책만 다르다**: `rel_key` 는 루트 밖이면 파일명으로 폴백하는데, 작업 링크에서 그
  폴백은 **다른 폴더의 동명 파일에 조용히 붙는다**. 작업용 변환은 폴백 없이 실패하고 기존
  절대경로를 유지한다 — "이 작업은 이식 대상이 아니다"를 정직하게 남긴다
- 마이그레이션 없음: 읽기는 새 키 없으면 옛 경로로 폴백, **저장할 때만** 승격. 읽는 김에 디스크를
  고치지 않는다(조용한 변이 금지)
- 화면 변화 0 — `PathTrack` 어포던스는 해석된 절대경로를 받는다. `template_media`·`work_mode`·
  `library_mode_of` 는 확장자만 보므로 무영향

**웹 표준 대조**(2026-07-29 조사) — 도출한 안이 확립된 해법과 일치한다:

| 우리 문제 | 표준 해법 |
|---|---|
| 참조가 PC에 묶임 | **path variable / named root + root-relative**(IntelliJ: 프로젝트는 변수 **이름**만 저장, 값은 각 머신 소유. AutoCAD xref: 공유·이동 프로젝트는 상대경로가 기본) |
| 남에게 전달 | **Collect / Package**(Ableton *Collect All and Save*, InDesign *Package*) — 평소엔 참조, 전달 시 자산을 모아 자기완결 폴더로. **§5.2 broad 안이 이 기능의 이름 없는 재발견** |
| 끊긴 참조 복구 | 자동 파일명 매칭은 **다른 폴더 동명 파일 오연결**로 악명(Pro Tools·Resolve·Premiere) → 수동 확인 선호. 위 폴백 거절 규칙과 같은 결론 |

표준은 경로를 **버리는** 것이 아니라 경로에서 **기계 고유 부분만 이름으로 치환**하는 것이다 —
id 키 안이 이 계열의 해법이 아닌 이유이기도 하다. **이 대조는 템플릿 축(판정 B)의 것이다**:
표의 세 문제는 전부 *"남에게 전달한다"* 를 전제하는데, 데이터 파일은 애초에 전달 대상이 아니라
대조 밖이다. 다만 3행(자동 파일명 매칭 = 오연결로 악명)의 결론은 이제 **두 축에서 같다**.

**범위**: 이 절의 **판정 A(개정)·B 는 U2 문서가 소유**한다 — §2.8 폐기와 §2.4 선행 갈림이
여기서 나오기 때문이다. 반면 **판정 C·D 의 구현은 U2 밖 「데이터 축 재편」 슬라이스**이고,
판정 B(템플릿 상대키)는 여전히 **부트스트래핑 broad 안의 선행**이다(§5.2). 이산 조치(필드 폐기 ·
풀 키 교체 · 마이그레이션 병합 확인 · 자동등록 제거)는 **GitHub 이슈**가 진다.

**착지 상태(2026-07-30)**: 그 「U2 밖 슬라이스」는 마일스톤 M 이 함께 진행해 **판정 C·D 가
착지**했다(#347 → #362 · `27f7b0a`, 파괴적). 그래서 §2.8 폐기의 전제와 §2.4·§4-E 의 선행이
충족됐고, 그 위에서 §2.4·§4-E 도 착지했다(#366 · `824b40a`). **판정 B 도 착지**(#348 → #368 ·
`18f84c4`)해 broad(#351)의 선행은 풀렸다 — 다만 #351 자체는 **별건으로 열려 있다**.

### ~~5.4 확인 2면의 형상~~ — **판정 이동 → §2.13**(형상 확정 2026-07-30 · 그 판정은 **조치완료** — #364 · `59d1d8a`)

§2.10 이 어휘·지목을 가져간 뒤 남은 것이 형상이었고 2026-07-30 에 닫혔다. **이 절이 적어 둔
「거울 + 드로어 2 pane」은 기각됐다** — 실측이 두 표의 **행이 이미 같음**을 보였고, 집합축은
pane 이 아니라 **표지 하나와 줄 하나**로 접힌다(§2.13). 곁들여 필드축 ack 게이트가 폐기되면서
아래 「합칠 짝」의 전제 자체가 달라졌다.

**아래는 그 판정이 살려 둔 근거다** — 작업대 동형을 기각한 이유와 렌더러 관계는 **그대로
유효하고**, §2.13 이 그것을 딛고 선다. 지우지 않는 이유는 1 pane 이 「작업대를 베낀 것」으로
오해되면 죽인 개념이 다시 들어오기 때문이다.

**실측 — 같은 일을 하는 pane 이 셋인데 세 표면에 흩어져 있다.**

| pane | 무엇을 보이나 | 온나라 작업대 | HWPX 「문서 만들기」 |
|---|---|---|---|
| 규칙 | 필드 연결·표시 | 좌 pane(`wb-left`) | **편집기** — 별도 몰입 화면 |
| 레코드 | 이 한 건의 실물 | 우 pane(`wb-right`) | **미리보기 드로어** — 모달 |
| 집합 | 선택 N건을 접은 상태 | **없다**(건별 큐라 불필요) | 거울 존(인라인) + 펼침 면 |

**U2 안(편집기 + 드로어 = 작업대 동형)은 기각한다.** 작업대 좌 pane 은 「저장하지 않은 변경」을
굴리는 pane 인데(`#wbDirtyNote`·`#wbSaveRules`), HWPX 쪽에서 그 축의 이름이 `runOverrides`
이고 **§10.14 에서 기각·사망**했다. 되살려도 안 붙는다 — HWPX 승인은 규칙 지문에 결속돼
있어(`approval_key = rules_key|selection_key`) 같은 면에서 규칙을 고치면 방금 받은 승인이
자동 무효가 되고, 한 면 안에서 승인→편집→무효→재승인이 돈다. 작업대는 승인 게이트가 없어
(복사는 건별) 그 상극이 안 생긴다. **형상만 베끼면 죽인 개념이 승인 모델과 부딪친다.**
편집 왕복 자체는 이미 계약으로 지어져 있다 — `previewEdit` 이
`return_context:{surface:"preview", reopen_drawer:true}` 와 증거 「보고 있던 행」을 들고
나가고 행별·파일 이름 「수정」은 편집기 해당 탭까지 딥링크한다(§10.14.3).

**~~U1 안(거울 + 드로어)이 합칠 짝이다~~** — **관찰은 참이고 결론이 기각됐다**(→ §2.13). 둘이
**같은 산출의 두 투영**인 것은 맞다(`screen_job.py:1338`: *"선택분 매핑 적용은 1회 — 파일명
미리보기와 거울 값이 공유"*). 틀린 것은 그 관계를 「접은 것 / 편 것」으로 적은 대목이다 — 두 표의
**행이 이미 똑같이 필드**라 접기·펴기가 아니라 **값 열 하나의 차이**였다. 그래서 「나란히 놓아
차이를 보이게」의 답이 2 pane 이 아니고, 집합축은 애초에 pane 을 받을 만큼의 정보가 아니었다.

**~~2탭이 아니라 2 pane 이다~~** — 2탭 기각은 유효하나(두 축을 동시에 못 본다) 2 pane 도 함께
기각됐다. *"골격 비용은 이미 지불돼 있다"* 는 **반만 참이었다**: 실 DOM 슬롯 이동 기법은 재사용
가능하지만 `#jobConfirmSheet` 의 2 pane 은 **이미 둘 다 점유**돼 있었다(거울 + `#jobRestate`).
그리고 인라인 거울 존을 **존치**시킨 근거(§10.6 결정 3 — ack 게이트의 유일 출구)는 §2.13 의
ack 폐기로 **소멸**하므로, 존은 존치가 아니라 **한 줄로 축소**된다.

**이 재편의 진짜 정체는 렌더러의 선행 정지작업이다**(사용자 지적, 2026-07-29). 작업대 우 pane 은
**이미 렌더러**다 — 「채운 모습 / 원문」 토글 + 대상 글꼴 선언 + 정렬 린트(`wbLint`)가 "붙여넣을
곳에서 어떻게 보이는가"를 그린다. HWPX 미리보기가 필드×값 표인 유일한 이유는
`screen_job.py:1029`·`job.js:456` 이 못 박은 전제 *"hwpx 본문은 앱에서 렌더 못 한다"* 하나이고,
그 전제가 뒤집히면 **두 pane 은 구분되지 않는다**. 부속은 이미 같은 자리에 같은 것이 붙어 있다
(레코드 이동 `wbPosition`↔`previewPos`, 곁줄 `wbLint`↔`previewEvidence`, 산출 표지
`wbReview`↔`previewFilename`).

수렴에 **포함되지 않는 것 둘**(렌더러가 와도 안 없어진다):

- **집합축은 배치 도구의 성질이다.** HWPX 는 N건을 한 번에 만들어 접힌 뷰와 ack 게이트가 계속
  필요하다. 그래서 최종 형상은 「작업대와 같은 화면」이 아니라 **작업대 우 pane + 집합 pane**
- **작업대의 렌더는 산출물 그 자체**(붙여넣으면 그 텍스트)인데 **HWPX 의 렌더는 산출물의
  그림**이다. 그래서 HWPX 쪽에는 파일 이름·저장 경로가 pane 에 반드시 붙고 작업대엔 그 축이 없다

**렌더러 슬라이스가 함께 지는 의무**: 부분 충실도 렌더는 **조용히 틀린 그림**이다. 텍스트 렌더는
글꼴 폭 문제를 린트로 정직하게 말할 수 있지만, HWPX 레이아웃을 근사로 그리면 "봤는데 다르게
나왔다"가 성립한다 — 근사·실패 구간을 시끄럽게 표시하는 몫이 렌더러에 붙는다.

**순서 조건은 충족됐다** — §2.1 회귀 복구(#330)와 §2.2~2.4 착지(#331~#333)가 끝났고, 그래서
2026-07-30 에 이 항목이 열렸다. ~~§5.2 의 splitter 항목은 여전히 같은 면의 기준면을 건드리므로
§2.13 착수와 함께 판단한다.~~ — **그 항목은 같은 날 기각됐다**(§5.2). 함께 판단할 것이 없어졌으
므로 §2.13 착수는 이 조건을 지지 않는다. 다만 §2.13 이 인라인 거울 존을 한 줄로 줄이면 **좌열이
짧아진다**는 사실은 남고, 그것이 sticky 기각의 근거 하나였다(붙일 것도 스크롤할 것도 줄어든다).

**~~계약 비용~~** → **§2.13 이 소유**(`#previewModal` 시트 승격 · 정적 DOM 계약 선행 갱신 ·
프로브 재작성 · 공용 「레코드 pane」 계약 판단).

---

## 6. 검증

이 표는 **방법**이고 실행 증거는 각 착지 PR(§1.1 대장)이 진다 — 방법 행에 완료 표식을 달지
않는 이유다. 여기서 초록을 선언하면 문서가 테스트의 정본인 척하게 된다.

> **매체 경보(2026-07-30 실측) — 커밋된 101 스크린샷이 조치를 안 담았다.**
> `examples/quickstart-101/img/08-session-panel.png` 은 액션바 버튼을 「미리보기」로 그리는데,
> **그 이미지를 커밋한 바로 그 커밋**(`a4ab7c1`, #333)의 `web/index.html:372` 는 이미
> 「생성 값 미리보기」다(#332 착지분). 즉 그림이 **옛 빌드 산출**이라 아래 「101 하니스」 행 셋
> (§2.1 회귀 복구 · §2.6 줄바꿈 · §2.10 눈검증)은 지금 상태로는 **낡은 그림을 대조**한다.
> 재생성이 선행이고, **라벨 대조가 그 매체의 양성 대조**다 — 「생성 값 미리보기」가 안 보이면
> 하니스가 안 돌았거나 옛 빌드를 찍은 것이다. (모션 층이 `reduce` 기기에서 영영 초록이던 것과
> 같은 계열: **계측 매체가 무엇을 찍었는지 먼저 묻는다.**)

| 대상 | 방법 |
|---|---|
| §2.1 회귀 복구 | 101 하니스 스크린샷 재생성 후 `examples/quickstart-101/img/01·08·12` 대조 — 존 구분선·패딩·캡션 블록화 확인 |
| §2.1 그물 | **복구 전에 빨간불을 먼저 본다**(양성 대조). 프로브를 양성 대조 없이 붙이면 그 프로브의 침묵이 무엇의 증거인지 말할 수 없다 |
| §2.2~2.4 | 실앱 한 바퀴(§10.9.5 후속 규칙 2): 데이터 선택 → 작업 선택 → 본문 확인 → 미리보기 → 생성 → 작업대 |
| §2.3 드로어 문안 | 페이로드 회귀 — `scope` 키 소멸, `note` 0건 시 빈 문자열 |
| §4 | 후보 카드 DOM 계약·action registry·`UI_CONTRACT.md` 갱신 + selftest 프로브(활성 카드 ⋮ 존재·경고 카드 클릭이 선택이 아님) |
| §4-E 전제 | `DEFERRED_ENTRY_REASONS` 축소가 `entry_reason_or_raise` fail-closed 테스트를 깨지 않는지 |
| §2.6 | 101 하니스 스크린샷 — **수동 행과 제안 행의 높이가 같은가**(줄바꿈 해소) + `↻` 가 두부로 떨어지지 않는지 실자형 확인 |
| §2.7 | selftest 프로브 신설 — 찾아보기 마운트 뒤 `#dataPickerModal` 이 **열려 있고** `#dataPickerPin` 이 **가시**인지(프로브 `click` 은 hidden 을 통과하므로 가시성까지 단언). `#dataPickerRegister` id 소멸은 정적 DOM 계약에 **먼저** 반영 |
| §2.9 | 무장 선택을 든 job 세션에서 `close_guard_state()["armed"]` 가 **False** · 같은 상태에서 workbench·editor 는 여전히 무장 · `session_guard_for`(홈 삭제)와 `_do_guard_state`(재겨눔 사전 확인)는 **불변** — 소비자 셋 중 하나만 끊겼는지 |
| §2.9 배제 선언 | `silent_loss_by_contract` 표에 `job` 이 있고, 그 표의 이름이 실제 컨트롤러에 있는지(`not_session` 의 stale 방지 단언과 같은 격) |
| §2.10 어휘 | 규칙축 4자리(표지·게이트·드로어 부제·승인 버튼)에 「확인」이 **한 곳도 안 남았는지** + 필드축(거울 캡션·안내·배지·빈 값 게이트)은 **전부 「확인」 그대로**인지. 한쪽만 바꾸면 축이 갈린 게 아니라 뒤섞인다 |
| §2.10 사본 | `app.py:2156` 셀프테스트 스텁의 게이트 문안이 `review_gate_text` 와 **같은 문자열**인지 — 갈리면 게이트가 스텁에서만 옛말을 한다 |
| §2.10 전제 | **겹침이 작업당 1회**라는 전제의 회귀 방어: 한 번 완주한 작업에서 승인 요구가 서지 않고 ack 만 남는지. 이 전제가 깨지면 국소수술을 택한 근거가 무너진다 |
| §2.10 눈검증 | 101 하니스 — 첫 실행 작업에서 ack 게이트 → 승인 게이트가 **다른 이름의 두 단계**로 읽히는지 |
| §2.11 기하 | 눌림 유지 중 **행 첫 칸 좌변 = sticky `th` 좌변**(이탈 0) · 행 폭·글자 상자 불변. `prefers-reduced-motion` 을 **`no-preference` 로 강제**해서 잰다 — 개발 기기가 `reduce` 라 강제 없이는 프로브가 영영 초록이다 |
| §2.11 대조 | 같은 프로브를 `reduce` 로도 돌려 **음성**(Δ 0)을 먼저 확인한다. 두 값이 다르지 않으면 프로브가 아무것도 못 재고 있다는 뜻이라 결론을 내지 않는다 |
| §2.11 계약 | 정적 계약이 「작은 조작 상자」와 「전폭 행」을 **가른 채로** 단언하는지 — 평면 8선택자 목록이 남아 있으면 다음 사람이 행에 `scale` 을 다시 넣는다 |
| §2.12 어휘 | `web/`·`src/` 전수에서 사용자 문안의 「휴지통」이 **0건**인지(주석·docstring 은 제외 — 백엔드 보존을 설명하는 산문은 살아야 한다). 자리 6 을 단언하는 `test_webapp_template.py` 사본이 같이 갈렸는지 |
| §2.12 선언된 미완 | `soft_delete`·`_purge_trash`·템플릿 `.trash` 컷오프가 **살아 있고**, 「도달 표면 없음」이 주석과 테스트 이름에 적혀 있는지 — 소비자 0인 코드를 다음 사람이 죽은 코드로 읽지 않게 하는 유일한 장치다 |
| §2.13 침묵 금지(**최우선**) | ack 폐기 뒤 **빈 값 구성이 바뀌면 승인이 자동 무효**가 되는지 — 한 번 완주한 작업에 새 데이터를 올려 빈 값이 새로 생겼을 때 게이트가 **서야** 한다. 이 단언이 없으면 폐기가 조용한 통과를 만든다(§10.12 판정 N 이 승인을 작업당 1회로 만든다) |
| §2.13 표식 생존 | `MISSING_MARKER` 가 붙는 조건이 「빈 값이 있으면」으로 재정의된 뒤에도 **생성·미리보기·승인 세 자리가 같은 술어**를 쓰는지(`_run_marker` 단일 출처). 갈리면 승인한 것과 생성된 것이 다른 문서가 된다 |
| §2.13 값 표면 단일화 | 「값을 말하는 표면」이 확인 면 **하나뿐**인지 — 인라인 존이 표를 잃고 한 줄이 됐는지. 존이 값을 계속 말하면 C3 원인이 그대로 남는다 |
| §2.13 상속 의무 | 데이터 전환 가드 문안에서 **ack 성분이 걷혔는지**(`acked_count` 소비자 — 존재하지 않는 손실을 말하지 않게) · `app.py:1362` `confirm_dispatch` 프로브가 죽은 액션을 단언하지 않는지 |
| §2.14 착지 | **filename 까지 전진한 뒤** 저장하는 신규 흐름에서 착지 탭이 `filename` 인지 — 기존 `_save_named` 경로(binding 에서 저장)는 그 자리를 안 밟아 조치 전후로 초록이다. txt 는 착지가 `binding` 그대로인지(마지막 탭이 곧 제자리) |
| §2.14 방어 불변 | 착지가 여전히 **저장본 편집 세션**인지(`editing_origin` 설정 · `has_unsaved_work()` False · notice(ok) 도착) — 되깎기가 아님의 단언. 빈 마법사 방치·`#save-msg` 증발이 재발하지 않는다 |
| §2.15 순서 불변 | **복사 뒤 `display_order()` 가 불변**인지(모델 층 단언) · 점 띠 `index_map` 의 자리가 그대로이고 **색만** 바뀌는지 · `position` 과 `can_prev/can_next` 가 **한 순서**를 쓰는지(두 값이 갈릴 이유가 사라졌다) |
| §2.15 전진 | `#wbAdvance` 켠 채 5번을 복사했을 때 **미처리 2번이 있으면 2번으로** 가는지(현행 규칙 유지) · 미처리 0이면 머무는지. 손 이동 `‹ ›`(`step`)는 **표시순** 그대로인지 |
| §2.16 일괄 | `.hwpx`·`.txt`·무관 파일이 섞인 폴더에서 **매체별 라우팅**(라이브러리 / 텍스트 레지스트리) · 동명 2건이 `(2)` 접미로 · **확정 전에는 홈에 아무것도 안 쓰는지**(#284 1항과 같은 잣대) · 하위 폴더 파일이 **안 들어오는지** |
| §2.16 무잔재 | 중간 1건 실패를 주입해 **앞선 성공분은 남고 실패분의 부분 파일은 사라지며** 결과가 건수·사유를 말하는지 — 하나가 죽어 열이 안 들어가면 일괄이 아니다 |
| §2.16 비채택 | 편집 세션이 열린 채 폴더 가져오기를 해도 **세션·`dirty` 불변**이고 세션 확인이 뜨지 않는지 — 「넣기」와 「쓰기」가 갈렸다는 단언 |
| §2.17 두 값 대조 | `dirty_discard_shown` 을 **비활성 판정으로 승격**하고 `dirty`/clean **양쪽**을 잰다 — 상시 표시가 되면 「존재」 단언은 무엇을 밀어 넣어도 참이라 조용히 죽는다. 저장·버리기가 **같은 술어**(`s.dirty \|\| pendingFieldEdit`)를 쓰는지도 여기서 본다 |
| §2.18 2분기 | 같은 결과에 대해 **작업 전환·데이터 교체 → 존이 닫히고** 로그에 퇴장 한 줄(경로 포함) · **선택·규칙 변경 → 강등으로 남는지**. 한쪽만 재면 성분별로 갈랐다는 단언이 안 선다 |
| §2.18 자기모순 방어 | 「실패한 N건만 선택」을 눌러 선택이 갈려도 **그 결과가 살아 있는지** — 판정 G 가 막으려던 자리다. 이 단언이 빨간불이면 성분 분기가 잘못 그어진 것이다 |
| §2.18 파기 대칭 | 「결과 닫기」(명시 파기)는 **로그 한 줄을 안 남기는지** — 치우라는 행동이 흔적을 남기면 반만 듣는 것이 된다 |
| §2.19 숨김≠제외(**최우선**) | 열을 숨긴 채 생성해 **그 열의 값이 문서에 그대로 들어가는지** · 「전체 열 검색」이 숨긴 열도 매치하는지 · 숨김 표지가 **상시** 서고 문안이 축을 말하는지. 이 단언이 없으면 선별이 조용한 오생성 경로가 된다 |
| §2.19 두 면 | 인라인은 선별을 따르고 **⤢ 시트는 전 열**인지 · 시트로 이사한 열 패널에 「숨기기」 항목이 **없는지** · 선두 식별 열은 **숨길 수 없는지** |
| §2.19 수명 | 데이터 교체에서 선별이 **소멸**하는지(필터 재생성과 같은 계층) · durable 저장이 **0곳**인지 |
| §2.20 신설 | 라이브러리 상세에서 「열기」·「폴더에서 보기」가 **실제로 그 템플릿 파일을 겨누는지**(payload `template_path` 신설이 선행) · 편집기 「템플릿」 탭의 같은 어포던스가 **그대로 사는지**(사본을 셋에서 둘로 줄인 것이지 옮긴 것이 아니다) |
| §2.5 동반 | 실앱 한 바퀴 — 신규 작업을 끝까지 저장하고 **완결 신호 넷**(notice·버튼 비활성·칩·헤더 탭 전환)이 한 면에서 읽히는지. 안 보이면 별건 승격(안내 자리 이동은 이번 조치에 넣지 않는다) |
| 전체 | `pytest` 전량 + 커버리지 플로어 + ruff·pyright + 101 하니스 |
