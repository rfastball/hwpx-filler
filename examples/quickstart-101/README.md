# hwpxfiller 101 — GUI로 첫 문서 만들기

화면(GUI)만으로 따라가는 입문 예제다. **한글 CSV** 한 벌로 두 가지를 만든다 —
**HWPX 완성 문서**(작업으로 저장 → 실행)와 **즉시 기안 텍스트**(바로 복사). 이 폴더
하나가 자족형이라, 여기 있는 파일만으로 처음부터 끝까지 화면에서 실습된다.

> 이 튜토리얼은 **CLI 를 쓰지 않는다.** 실무자는 앱으로 일한다는 전제다.
> 자동화용 명령줄이 필요하면 [루트 README](../../README.md) 의 CLI 절을 보라.

## 이 폴더에 뭐가 있나

```
quickstart-101/
├─ start-101.cmd             ← 더블클릭 = 이 폴더를 홈으로 앱 실행
├─ reset-101.cmd             ← 더블클릭 = 실습 생성물·설정만 지우고 처음 상태로
├─ README.md                 ← 지금 이 문서(101 따라 하기)
├─ PATTERNS.md               102 — 세 가지 실전 조합(여러 템플릿·여러 데이터·작업 전환)
├─ templates/                누름틀 템플릿(.hwpx) — "템플릿 관리" 화면에 자동으로 뜬다
│   ├─ 발주요청서.hwpx        6필드 템플릿
│   └─ 구매요청서.hwpx        4필드 부분집합(102 용)
├─ text_templates/           평문 {{필드}} 초안 — "기안" 화면에 자동으로 뜬다
│   ├─ 발주요청_기안.txt      정상 초안(6필드 전부 CSV 에 있음)
│   └─ 오류연습_미치환.txt    ⚠ 일부러 없는 토큰을 넣은 오류 연습용(정상 경로와 분리)
├─ data/                     채울 값(한글 헤더 = 필드 이름 → 직접 매칭)
│   ├─ 발주목록.csv           3건. utf-8-sig — 트랙 A·B 가 같은 파일을 쓴다
│   └─ 발주목록_2.csv         같은 헤더·다른 2건(102 용)
└─ make_template.py          위 템플릿·CSV 를 다시 만드는 스크립트(맨 아래 참고)
```

실습 중 앱이 만드는 것(작업·설정 등)은 이 폴더 밑 `jobs/`·`ui_settings.ini` 등으로 생기고
`.gitignore` 로 커밋에서 빠진다. **`reset-101.cmd`** 를 더블클릭하면 실습 생성물·설정만
지워져(예제 자산은 불가침) 언제든 처음부터 다시 시작할 수 있다.

## 자산 ↔ 배우는 것 대응표

| 자산 | 목적 | 배우는 기능 | 기대 결과 |
|---|---|---|---|
| `발주요청서.hwpx` + `발주목록.csv` | 첫 HWPX 생성 | 필드 확인 → 자동 제안 확정 → 작업 저장 → 실행 | 완성 HWPX 3건 |
| `구매요청서.hwpx` + 같은 CSV | 템플릿 재사용(102) | 한 데이터 → 여러 템플릿, 여분 열 무시 | 다른 서식 HWPX 3건 |
| `발주목록_2.csv` | 데이터 교체(102) | 여러 데이터 → 한 템플릿, 같은 폴더 누적 | 추가 HWPX 2건 |
| `발주요청_기안.txt` + 같은 CSV | 즉시 기안 | 레코드 이동·미리보기·복사 | 채워진 평문 초안 |
| `오류연습_미치환.txt` | 경고 표면 학습 | 데이터에 없는 `{{담당연락처}}` 토큰의 미치환 경고 | 빨간 토큰·`● 항목 없음` 신호 |

필드 ↔ 데이터 열 대응: 템플릿 필드명 = CSV 한글 헤더 **정확 일치**가 101 의 핵심이다
(그래서 매핑이 자동 제안된다). `발주요청서` 는 6열 전부(공고번호·수요기관·공고명·
추정가격·납품기한·담당자), `구매요청서` 는 그중 4개(수요기관·공고명·추정가격·담당자)만
쓰고 나머지 열은 무시된다.

## 준비 — 더블클릭으로 실행

**최초 1회만** 저장소 루트에서 의존성을 받는다(docs/DEVELOPMENT_ENVIRONMENT.md 참고):

```powershell
uv sync --locked --all-extras --group dev --group build
```

이후로는 이 폴더의 **[`start-101.cmd`](start-101.cmd) 를 더블클릭**하면 끝이다. 런처가
자동으로 이 예제 폴더를 앱의 홈(`HWPXFILLER_HOME`)으로 잡아 준다 — 그래서 실제
`~/.hwpxfiller` 를 건드리지 않으면서 **이 폴더의 템플릿·기안 초안이 화면에 바로 뜬다.**
(터미널이 편하면 `examples\quickstart-101\start-101.cmd` 로 실행해도 된다.)

<details>
<summary>런처 없이 직접 실행하려면 (수동)</summary>

저장소 루트에서:

```powershell
$env:HWPXFILLER_HOME = "$PWD\examples\quickstart-101"
python -m hwpxfiller.webapp
```

macOS/Linux: `HWPXFILLER_HOME="$PWD/examples/quickstart-101" python -m hwpxfiller.webapp`
</details>

## 화면 한눈에 — 왼쪽 탐색 레일

앱이 뜨면 왼쪽에 세로 메뉴(탐색 레일)가 있다. 위에서부터:

| 메뉴 | 하는 일 |
|---|---|
| **대시보드** | 저장된 **작업** 보관함(카드 목록) — 여기서 만들고 실행한다 |
| **작업 에디터** | 템플릿+매핑을 작업으로 저장하는 4단계 마법사 |
| **실행** | 저장된 작업에 데이터를 겨눠 문서 일괄 생성 |
| **즉시 기안 (txt)** | 텍스트 초안에 데이터를 채워 복사·저장(→ 아래 트랙 B) |
| **템플릿 관리** | 템플릿(.hwpx/.txt) 컴파일 상태·미리보기, "작업 만들기" |
| **데이터 관리** | 자주 쓰는 데이터(경로 참조)를 등록해 재사용 |

101 은 **대시보드**(트랙 A)와 **즉시 기안**(트랙 B) 둘만 쓴다.

---

## 트랙 A — HWPX 문서 만들기

앱의 뼈대는 **작업(Job)** 이다. 작업 = `{템플릿 + 매핑 + 파일명 패턴}` 한 벌에 이름을
붙여 저장한 것. **데이터는 작업에 저장하지 않는다** — 실행할 때마다 그 순간의 파일을
새로 고른다. 그래서 순서는 늘 **① 작업 만들기 → ② 실행**이다.

### ① 작업 만들기 (편집기 4단계)

1. **대시보드**에서 **[＋ 새 문서 작업]**(Alt+N) → *작업 편집기* 창이 열린다.
2. **1단계 — 템플릿 선택**: **[찾아보기…]** → `templates/발주요청서.hwpx` 선택 →
   "필드 6개…" 요약 확인 → **[다음]**.
   (이 예제 템플릿은 이미 누름틀이라 추가 변환 단계가 없다.)
3. **2단계 — 데이터 선택 (선택)**: **[찾아보기…]** 로 `data/발주목록.csv` 를 물리면
   다음 단계의 매핑 미리보기가 실제 값으로 채워진다. *이 데이터는 미리보기용일 뿐,
   작업에 저장되지 않는다.* 건너뛰려면 그냥 **[다음]**.
4. **3단계 — 필드 매핑 확정**: 템플릿 6필드가 표로 뜬다. 헤더가 필드명과 같아
   **데이터 항목**이 자동으로 맞춰져 있다. **[모두 확정]** 을 눌러 전 행을 확정 →
   **[다음]**. (자동 제안은 초안이라, 같더라도 한 번 확정해야 넘어간다 — "조용히
   추측하지 않는다"는 원칙.)
5. **4단계 — 작업 저장**: **작업 이름** 에 `발주요청서` 입력, **파일명 패턴** 을
   `발주요청서-{{공고번호}}` 로(기본값 `공고서-{{date}}-{{seq:001}}` 을 고쳐서) →
   **[작업 저장]**. (이 작업이 채우는 필드 이름만 토큰으로 쓸 수 있다 — 없는
   토큰은 실행 화면이 생성을 차단하고 알려준다.)

대시보드에 `발주요청서` **카드**가 생긴다(상태 배지 + **[실행] [작업 편집] [삭제]**).

### ② 실행 — 데이터 물려 문서 생성

1. `발주요청서` 카드의 **[실행]**(또는 카드 더블클릭) → *실행* 화면이 열리고,
   왼쪽 레일에 **"실행: 발주요청서"** 가 뜬다.
2. **데이터**: **[파일 선택…]** → `data/발주목록.csv`. (3건이 로드된다.)
3. **미입력 필드 확인**: 각 필드가 `✓ 채움` 으로 뜨는지 본다. 비어 있으면
   `● 필드 — 미입력 확인` 버튼을 눌러 직접 확인해야 생성이 열린다(아래 안전장치).
4. **생성 대상 레코드**: 만들 건을 체크(기본 전체). **[전체 선택]/[전체 해제]** 로 조절.
5. **저장 폴더**: 기본값(`templates/Results`)을 그대로 두거나 **[찾아보기…]** 로 지정.
6. **[문서 생성]**(Ctrl+Enter) → 진행바가 돌고 **"완료 — 성공 3/3 · 실패 0"**.
   끝나면 저장 폴더를 열지 묻는다.

`발주요청서-2026-001.hwpx` 등 3개가 저장 폴더에 생긴다. 한컴오피스로 열어 값을 확인한다.

### 알아둘 안전장치 (조용히 넘어가지 않는다)

원칙은 **"묻고 확정하게 하라, 아니면 시끄럽게 알려라"** — 애매하면 조용히 진행하지 않는다.

- **빈 값이 있으면 생성 버튼이 잠긴다.** 어떤 필드가 비면 `● … 미입력 확인` 배지로
  뜨고, 눌러서 `✓ … 미입력 표시 예정` 으로 직접 확인해야 **[문서 생성]** 이 열린다.
  확인하고 생성하면 그 자리에 `〘미입력·필드명〙` 표식이 들어가고, 완료 요약도
  "미입력 표시 필드 N개"라고 병기한다(빈칸인 채로 사라지지 않게).
- **같은 이름 파일이 있으면 덮어쓰기 확인.** 저장 폴더에 같은 파일명이 있으면
  **"덮어쓰기 확인"** 대화상자가 목록과 함께 뜬다. **[덮어쓰고 진행]** 을 누르지
  않으면 생성을 멈춘다(손보정했을 수 있는 기존 산출물을 말없이 파괴하지 않으려고).

---

## 트랙 B — 즉시 기안 (텍스트 초안 바로 채우기)

완성 문서(.hwpx)가 아니라, **평문 초안**을 값으로 채워 곧바로 메일·메신저·온나라
기안창에 붙여넣고 싶을 때다. 작업으로 저장하지 않고 **채워서 바로 복사**한다.

1. 레일에서 **즉시 기안**.
2. **템플릿** 콤보에서 `발주요청_기안` 선택. (이 폴더의 `text_templates/*.txt` 가
   자동으로 뜬다 — 홈을 이 폴더로 지정했기 때문.)
3. **데이터**: **[파일 선택…]** → `data/발주목록.csv`. (급하면 **[수기 입력…]** 으로
   한 건을 손으로 채울 수도 있다.)
4. **◀ / ▶** 로 레코드를 넘기며 오른쪽 **기안 미리보기**를 본다.
5. **[클립보드로 복사]**(또는 **[텍스트 파일로 저장…]**).

여기서도 **없는 필드는 조용히 사라지지 않는다.** 초안이 데이터에 없는 필드를 참조하면
그 `{{토큰}}` 이 미리보기에 **빨갛게 그대로 남고**, `● 항목 없음` 으로 신호한다 —
빈칸으로 새는 대신 눈에 보인다.

---

## 다음 걸음

| 하고 싶은 것 | 어디로 |
|---|---|
| 한 데이터→여러 템플릿 / 여러 데이터→한 템플릿 / 작업 전환 | [102 — 세 가지 실전 조합](PATTERNS.md) |
| 내 초안(평문 {{토큰}} hwpx)을 누름틀 템플릿으로 | **템플릿 관리** → 카드 **[누름틀 변환]** |
| 데이터 파일을 등록해 실행 때마다 재사용 | **데이터 관리** |
| 나라장터 API 로 채우기(영문키→한글필드) | [루트 README](../../README.md) 의 나라장터 절 |
| 더 큰 실사용 예제 | [`tests/corpus/scenario/`](../../tests/corpus/scenario/README.md) |

## (참고) 예제 자산 다시 만들기

`templates/*.hwpx` 와 `data/*.csv` 는 손으로 편집하지 말고 스크립트에서 파생한다
(같은 입력 → 같은 출력). 필드·데이터를 바꾸려면 `make_template.py` 안의
`FIELDS`·`RECORDS`·`TEMPLATES` 를 고치고 다시 실행한다.

```powershell
python examples/quickstart-101/make_template.py
```

`text_templates/*.txt` 는 평문이라 스크립트 없이 그냥 손으로 편집하면 된다
(단, 정상 초안의 토큰은 `FIELDS` 에 있는 이름만 써야 채워진다 —
`오류연습_미치환.txt` 만 예외로, 없는 토큰 `{{담당연락처}}` 를 **일부러** 갖고 있다).

자산과 문서가 어긋나지 않게 저장소 테스트(`tests/test_quickstart_101_assets.py`)가
필드·헤더·토큰 대응과 인코딩을 기계로 비준한다. 이 스크립트와 테스트는 **저장소 전용
도구**고, 학습 세트 자체(템플릿·CSV·txt·cmd 런처)는 이 폴더만 복사해도 자족한다.
